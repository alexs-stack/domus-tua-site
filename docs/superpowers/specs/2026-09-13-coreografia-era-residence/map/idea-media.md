# media
## currentState
VIDEO. Esiste UNA sola clip locale: public/media/domus-hero.mp4. ffprobe: 4,0 s, 100 fotogrammi a 25 fps, 1920x1080 H.264, 10,8 Mbps (5.404.642 byte), nessuna traccia audio, una traccia dati; il moov sta al byte 36, quindi il file è già faststart. In public/ non c'è nessun altro .mp4, .webm o .mov. È la ripresa col drone e ha il logo Domus Tua impresso in alto a sinistra (Congedo.tsx:110-113).

1) app/lib/media.ts. Configurazione unica: `enabled: false` con il commento «Scelta cliente (2026-08-03)» (media.ts:16-18), mp4 (:19), `webm: ""`, cioè la sorgente non viene resa (:20-22), poster e base /media/hero-raffaela.jpg (:23-27).

2) HeroCinematic, il video dell'hero. Oggi non si monta mai: l'effect esce subito se `!heroCinematic.enabled` (HeroCinematic.tsx:361). Se fosse acceso:
- si monta solo con gsap.matchMedia `motionOk and desktop`, cioè 768 px (:363);
- arriva dopo il primo paint, con requestIdleCallback (timeout 2500) o in ripiego setTimeout 1200 (:369-374);
- ha `autoPlay muted loop playsInline preload="metadata"` e onError torna alla foto (:431-444);
- non ha NESSUN gate sulla visibilità: una volta montato va in loop per sempre, anche quando la banda è fuori schermo;
- al revert del media query viene smontato (:382), quindi se ricompare riparte da 0.
La foto LCP con `preload` resta sempre sotto (:415-429).

3) Congedo, l'unico video che suona davvero. Il `<video>` è sempre nel DOM, senza autoPlay e senza poster, con `muted loop playsInline preload="none" aria-hidden` e `scale-[1.14] origin-bottom-right` (Congedo.tsx:108-124). Non ha `disablePictureInPicture` né `disableRemotePlayback`. Il gate è a mano:
- IntersectionObserver con `rootMargin: "40% 0px"` sulla section, più due matchMedia: `prefers-reduced-motion: no-preference` e `min-width: 768px` (:52-73);
- `sync()` chiama `v.play().catch()` se la banda è vicina e i due MQ sono veri, altrimenti `v.pause()` (:61-64). Viene rivalutato anche al change dei MQ (:73); nel cleanup ci sono disconnect e pause (:74-78);
- non tocca mai currentTime, quindi riprende dal punto in cui si era fermato (semantica di HTMLMediaElement);
- con preload none sotto 768 e con reduced-motion l'mp4 non viene mai chiesto e resta il poster next/image piscina-lusso.jpg (:20, :99-106);
- non controlla saveData, visibilitychange né gli overlay aperti (VideoLightbox registra `setOverlay("video")`, VideoLightbox.tsx:60-64; il registro è in lib/ui/overlays.ts:12-37).
Non c'è nessun test e2e sul gate: il grep di e2e/, dei test unit e di scripts/ non trova né `congedo` né `paused`.

4) LazyYouTubeEmbed è una facciata click-to-load. Prima del click c'è solo un poster (next/image o YoutubeThumb) con hover `scale-105` in 1200 ms (LazyYouTubeEmbed.tsx:58-59, :79-85). Il click monta l'iframe youtube-nocookie `?autoplay=1` (:49, :63-71), che poi resta montato. Nessun gate: se si scorre via il video continua a suonare, e senza enablejsapi non si può mettere in pausa dall'esterno. La privacy è documentata (:30-34). La usano:
- HorizonStory (Short 9:16 in `.dt-media-column--tall`, max 420 px, HorizonStory.tsx:207-214), senza motion;
- OpenDomus (poster di Teresa in `.dt-media-half aspect-video!`, OpenDomus.tsx:162-171), volutamente senza Parallax: «bersaglio mobile» (:155-156);
- /lavora-con-noi (LavoraConNoiContent.tsx:913-917), dentro Parallax -0.06 e Reveal (:900-919).

5) Voci. Nessun `<video>`: le tessere sono link a YouTube con YoutubeThumb in `.dt-media-full`, `.dt-still-trim` (scale 1.43) e play rosso (Voci.tsx:200-232), dentro un carosello nativo a snap senza motion (:183-187). Il click apre VideoLightbox (:205-209, :310).

6) FeaturedTestimonial. Foto statica in `.dt-media-half !aspect-video` con `.dt-still-trim--top` (1.32), dentro `<Parallax speed={-0.04}>` (FeaturedTestimonial.tsx:120-149); il click apre VideoLightbox (:107-111, :178).

7) VideoLightbox. L'iframe esiste solo a dialog aperto (VideoLightbox.tsx:93, :152-160). La chiusura lo smonta, quindi il video si ferma e alla riapertura riparte da 0. Il commento dice che non riproduce da solo e non parte allo scroll (:24-25).

COME ENTRANO LE FOTO OGGI.
- I tre moduli (globals.css:400-436): `.dt-media-full` 16:9, `.dt-media-half` 1:1, `.dt-media-column` 4:5 o `--tall` 9:16. Da 64rem valgono 42vw con max 640, sempre su cream-deep. Li presidia moduli-media.test.ts:49-71.
- Il gesto dominante è la deriva Parallax: speed×14 in yPercent, cioè ±0,56 % con speed -0.04; scrub true da `top bottom` a `bottom top`; corsa dimezzata sotto 768; will-change solo mentre il trigger è attivo (Parallax.tsx:55, :82-117; DESIGN.md:508). Chiamanti: Posizionamento.tsx:83-98, Paths.tsx:236-247, Method.tsx:225-236, Services.tsx:285-298 (con scale 1.03), Team.tsx:159-171, FeaturedTestimonial.tsx:120, PageHero.tsx:113-121 (`mobile={false}`, preload LCP).
- Reveal (fade-up di 2.5rem in 0,9 s, IO con threshold 0.12 e rootMargin -8 %, replay in entrambe le direzioni, rete a 2500 ms: Reveal.tsx:34-44, globals.css:508-521) avvolge qualche media: Contact.tsx:758-771, ChiSiamoContent.tsx:261-274, LavoraConNoiContent.tsx:901-918, Social.tsx:89-100 (iframe del feed). Il docstring di Reveal.tsx:15 dice ancora «de-blur», ma la CSS non ha blur; restano `filter: none` orfani in globals.css:530 e :538.
- Il sipario del territorio è l'unica apertura a clip su una foto: `[data-horizon-slide]` su `.dt-media-full` con hero-aerial.jpg (HorizonStory.tsx:306-315). clipPath va da `inset(0% 100% 0% 0%)` a `inset(0%)` e la scala interna da 1.15 a 1, in CURTAIN_DUR 1,6 s con ease dtOut (HorizonScroller.tsx:53-62).
  - Da lg: containerAnimation, start `left 90%`, restart all'ingresso e reverse su onLeaveBack (:580-601).
  - In colonna: start `top 70%`, stessi valori (:334-360).
  - Il codice dei due rami è duplicato.
- StarReviews: la foto premio-team.jpg (StarReviews.tsx:86, :667-673) è ritagliata a stella con un poligono a 10 vertici, rigenerato per frame da un proxy {cx, cy, r} (:367-368). Il lampo è in sola opacity (:674-682); will-change: clip-path solo durante il morph (globals.css:1994-2001).
- Rotaia del team: pan interno ±4 % (`.dt-rail_pan`, Team.tsx:234-244; DESIGN.md:523).
- Hero (HeroCinematic.tsx:415-429) e Congedo (poster fisso a scale 1.14) non hanno ingresso.
## eraExact
PLAY/PAUSE, `initPlayPauseVideoScroll` (reverse-engineering/era-residence/js/main.pretty.js:384-399).
- Per ogni contenitore `[data-video-playpause]` prende i figli `.video`; al boot fa `e.load(); e.currentTime = 0` (:388).
- `ScrollTrigger.create({ trigger: contenitore, start: "top bottom", end: "bottom top" })` (:389-392): onEnter e onEnterBack chiamano `play().catch(()=>{})` (:393-394), onLeave e onLeaveBack chiamano `pause()` (:395-396).
- Alla pausa il tempo non viene azzerato, quindi la clip riprende da dove si era fermata.
- Markup (html/index.full.html:1185-1238): tutti i `<video muted playsinline loop disablepictureinpicture webkit-playsinline poster=…avif class="video">` sono SENZA autoplay. Le sorgenti sono webm VP9 con alfa più mov HEVC con alfa (README.md:441-444).
- Contenitori gated: loc-scroll-area, apart-info-w, interior-s_l_img-w, arch-scroll-area, other-w.
- Safari: vengono tolte le `source[src$=.webm]` e i video fuori da `.hero-w`/`.cta-w` ricevono load()+play() (main.pretty.js:737-739).
- Le sole clip d'ambiente di Era sono i fiori bougainvillea_01…07 (README.md:448-452), asset proprietari.

ANIMATORE `slide`, `animateSlide` (main.pretty.js:620-672).
- Reveal: il wrapper va da `clipPath: "polygon(100% 0%, 100% 0%, 101% 100%, 125% 100%)"` a `"polygon(0% 0%, 100% 0%, 100% 100%, 0% 100%)"`, durL 1.2, delay delayReveal 0.3, ease "InOut" = 0.75,0,0.25,1 (:626-633). In parallelo il firstElementChild va da `scale: 1.5, xPercent: 25` a `scale: 1, xPercent: 0` con la stessa durata ed ease (:634-644).
- Hide, a durata PIENA (durL, non durS): da `polygon(0% 0%, 100% 0%, 125% 100%, 0% 100%)` a `polygon(0% 0%, 0% 0%, 0% 100%, 0% 100%)`, inner a `scale 1.5, xPercent -25` (:646-662).
- Initial: `clipPath: "inset(100% 0% 0% 0%)"`, inner `scale 1.5, xPercent 25` (:664-670).
- Innesco (:909-917, :1009-1024): raggruppamento per `[data-scroll-reveal="w"]`, `visibility: visible`, ScrollTrigger `start: "top bottom"` (nello scroller orizzontale `containerAnimation` e `"left bottom"`), `once: true`.
- Usato su `.img-w` nella sezione interior (index.full.html:1216) e nello slider benefits (html/benefits-slider.html:65).
- README §7 (README.md:331, :333): lo hide di slide è a durL, gli altri animatori a durS.

CLIP A LINEA, `animateLine` (main.pretty.js:588-618).
- Reveal da `clipPath: "inset(0% 0% 100% 0%)"` a `"inset(0% 0% 0% 0%)"`, durL 1.2, ease Out (0.25,1,0.5,1), stagger 0.1.
- Hide a `"inset(100% 0% 0% 0%)"`, durS 0.4, ease In, stagger 0.05.
- Initial `inset(0% 0% 100% 0%)`.

TENDINA DELLA MAPPA, `.img.loc-path` (main.pretty.js:2653-2669): da `clipPath: "inset(0% 100% 0% 0%)"` a `"inset(0% 0% 0% 0%)"`, `duration: 2*durL` (2,4 s), ease Out, delay durM 0.8, containerAnimation con `start: "left bottom"`, `once: true` (README.md:426). È l'antenato diretto del sipario del territorio di Domus.

PARALLASSI (main.pretty.js:821-905). Tutte hanno trigger `closest('[data-parallax="w"]')` e si spengono con `data-mob="off"` sotto 992 o `data-desk="off"` sopra.
- `data-parallax="img-in"` (:857-874): `yPercent -20 → 0`, translateZ 10, ease none, `start: "top bottom"`, `end: "bottom bottom"`, `scrub: true`.
- `img` (:822-838): yPercent -15 → 15, `start: "top bottom"`, scrub 0.5.
- `img-out` (:839-856): 0 → 20, `bottom bottom → bottom top`, scrub 0.5.
- `ctn-down` e `ctn-up` (:875-904): ±10, `top 125% → bottom -25%`, scrub 0.5.

TOKEN (README.md:88-97): durS 0.4, durM 0.8, durL 1.2, stagger 0.1, delayReveal 0.3; InOut 0.75,0,0.25,1; Out 0.25,1,0.5,1; In 0.5,0,0.75,0.
### P1 — Gate unico per i video d'ambiente: useAmbientVideo (Congedo oggi, hero se riacceso) [adopt-with-conditions]
kind=refine-existing sticky=False files=app/components/hooks/useAmbientVideo.ts (nuovo), app/components/Congedo.tsx, app/components/HeroCinematic.tsx, app/lib/ui/overlays.ts, e2e/home.spec.ts dir=DESIGN.md:401 (768 per il video del congedo),DESIGN.md:559-560 (banda video di congedo),HeroCinematic.tsx:347-353 (ricetta saveData),media.ts:16-18 (hero spento per scelta cliente 2026-08-03: non si riaccende qui)
ADATTAMENTO: RIFINITURA di ciò che esiste, nessun gesto nuovo. Si estrae il gate a mano del Congedo (Congedo.tsx:52-79) in un hook, app/components/hooks/useAmbientVideo.ts, con firma `useAmbientVideo(videoRef, hostRef, { rootMargin = "40% 0px", minWidth = "(min-width: 768px)" })`.

Il video suona solo se sono vere tutte queste condizioni:
- host vicino allo schermo (IntersectionObserver);
- `prefers-reduced-motion: no-preference`;
- minWidth;
- `!navigator.connection?.saveData` (la ricetta scritta in HeroCinematic.tsx:347-353 ma mai applicata);
- `document.visibilityState === "visible"`;
- nessun overlay che copre la pagina.
sync() chiama `v.play().catch(()=>{})` oppure `v.pause()` e non tocca MAI currentTime, quindi la clip riprende dove si era fermata, come Era (main.pretty.js:393-396).

Ascoltatori: IO, change dei due matchMedia, visibilitychange, registro overlay. Per l'overlay serve un'estensione minima di lib/ui/overlays.ts, per esempio `isOverlayOpen(name)` più una notifica a ogni set: oggi `subscribeOverlays` avvisa solo quando «c'è qualcosa aperto» cambia (overlays.ts:22-25) e `hasOverlay()` è vero anche col banner cookie. Il Congedo va messo in pausa solo con "video" (VideoLightbox copre con bg-cream/95, VideoLightbox.tsx:113) e con "quick-look", MAI con "cookie-consent".

Congedo:
- sostituire l'effect col hook;
- aggiungere `disablePictureInPicture` e `disableRemotePlayback` al `<video>` (Congedo.tsx:108-121), come l'attributo di Era (index.full.html:1185), perché la clip è decorativa (aria-hidden);
- restano preload="none", nessun autoPlay, il poster next/image sotto, rootMargin 40 % e la soglia 768 (DESIGN.md:401).

HeroCinematic, solo se la cliente riaccende `enabled` (media.ts:16-18): togliere `autoPlay` (HeroCinematic.tsx:435) e usare il hook con `rootMargin: "0px"`. Così la banda di 60svh smette di decodificare un 1080p a 10,8 Mbps appena esce dallo schermo, mentre oggi andrebbe in loop per sempre. Il mount dopo idle (:369-374) e il matchMedia motionOk+desktop (:363) restano; si resta nel ramo che monta il video, quindi LCP e patto della porta non cambiano.

Reduced-motion: nessuna chiamata a play, 0 byte di mp4, resta il poster. Sotto 768 idem. Con saveData idem a ogni larghezza.
RISCHI: Registro overlay: con hasOverlay() globale il banner cookie della prima visita terrebbe il Congedo in pausa. Serve il filtro per nome, oppure la clip non riparte finché non si accetta. / play() rifiutato (iOS Risparmio energetico, autoplay policy): il catch c'è già e sotto resta la foto poster (Congedo.tsx:99-107), niente schermo vuoto. / visibilitychange più IO possono chiamare play() due volte di fila: play() è idempotente, ma la promise rifiutata va sempre catturata. / Oggi nessun test copre il gate: una regressione (per esempio autoPlay reintrodotto) passerebbe inosservata.
TEST: Nuovo e2e in e2e/home.spec.ts. Progetto desktop con reducedMotion no-preference: scorrendo al Congedo `video.paused === false`; tornando in cima `paused === true`; al ritorno currentTime è maggiore o uguale al valore letto prima (ripresa, non riavvio). / Nuovo e2e a 390 px e con reducedMotion reduce: nessuna richiesta che finisca in .mp4 (page.on('request')). / Nuovo e2e: aprendo VideoLightbox dalla testimonianza, il Congedo in vista va in pausa. / Suite: npm run test:e2e sulla porta 3177; gli snapshot sono solo darwin (memoria domus-testing-windows).
VERDETTO: Rifinitura dentro l'involucro attuale: nessun gesto nuovo, nessuna sezione sticky, nessun cambio visibile. L'idea dell'articolo (clip in pausa fuori schermo, ripresa dal punto di pausa) c'è già quasi tutta. Il Congedo ha un gate IO più due matchMedia e non tocca mai currentTime (Congedo.tsx:52-79). Il delta vero è piccolo: saveData, visibilitychange, gli attributi disablePictureInPicture/disableRemotePlayback e un test e2e che oggi manca.

La parte sugli overlay non regge alla verifica:
- nessun componente chiama setOverlay("quick-look"): CaseQuickLook non registra overlay e sta solo in PropertySearch, fuori dalla home;
- VideoLightbox si apre solo da Voci e da FeaturedTestimonial. In page.tsx fra la testimonianza e il Congedo stanno Social, Team (rotaia con runway) e Contact, quindi quando un lightbox può aprirsi il Congedo è già fuori dal rootMargin 40 % e già in pausa. Estendere overlays.ts vuol dire API nuova per un caso che non esiste.

Anche la parte HeroCinematic tocca codice spento (media.ts:16-18) senza nessun test che lo copra. LCP, patto della porta e reduced-motion restano intatti.

Verificato sull'ambiente di test:
- il Chromium di Playwright (151, Chrome for Testing) risponde "probably" a H.264, quindi l'e2e sul play è fattibile;
- la guardia e2e ignora net::ERR_ABORTED (e2e/helpers.ts:95), quindi le richieste di range annullate alla pausa non rompono guards.failedRequests.
CONDIZIONI: Togliere l'estensione di lib/ui/overlays.ts e la pausa su "video"/"quick-look": nessun apritore di VideoLightbox sta vicino al Congedo (page.tsx:81-92) e "quick-look" non è mai registrato. Si riconsidera solo se un apritore finisce entro il 40 % della banda. / Estrarre useAmbientVideo solo se si tocca davvero anche HeroCinematic. Finché enabled resta false (media.ts:18), arricchire l'effect di Congedo.tsx:52-79 senza un hook con un solo consumatore. / Non modificare HeroCinematic.tsx (autoPlay a :435) finché il video dell'hero non viene riacceso da chi l'ha spento. Se lo si fa ora, lasciare invariato il ramo montato dopo idle (:369-374) e il matchMedia motionOk+desktop (:363). / Invariati: preload="none", nessun autoPlay, poster next/image sotto, rootMargin "40% 0px", soglia 768 (DESIGN.md:401), nessuna scrittura di currentTime. / Il gate saveData legge navigator.connection?.saveData con optional chaining: Safari e Firefox non hanno l'API e devono comportarsi come oggi. / Test e2e al posto di quelli proposti: (a) progetto desktop-1440 con goto che salta l'intro, scroll al Congedo, video.paused === false, ritorno in cima, paused === true, poi currentTime >= valore letto; (b) progetto mobile-390 e test.use reducedMotion "reduce" su desktop: nessuna richiesta che finisca in .mp4. Il test «apro VideoLightbox e il Congedo va in pausa» va tolto: non è realizzabile.
EVIDENZE: Congedo.tsx:52-79: gate IO rootMargin "40% 0px" + MQ reduced-motion e 768; sync con play().catch / pause; cleanup con disconnect e pause / Congedo.tsx:108-124: <video> muted loop playsInline preload="none" aria-hidden, senza disablePictureInPicture/disableRemotePlayback / HeroCinematic.tsx:361 esce se !enabled; :435 autoPlay; :347-353 ricetta saveData mai applicata / lib/ui/overlays.ts:12 OverlayName comprende "quick-look"; :22-25 notifica solo al cambio vuoto/non vuoto / grep setOverlay in app/: solo CookieConsent.tsx:180, Header.tsx:121, VideoLightbox.tsx:62. CaseQuickLook.tsx non registra overlay, lo importa solo PropertySearch.tsx:14 / VideoLightbox montato solo in Voci.tsx:310 e FeaturedTestimonial.tsx:178 / app/page.tsx:81-92: Voci … FeaturedTestimonial, Social, Team, Contact, Congedo / e2e/helpers.ts:95 ignora net::ERR_ABORTED / Chromium Playwright 151: canPlayType('video/mp4; codecs=avc1.640028') = probably, webm vp9 = probably / playwright.site.config.ts: reduced motion non globale, progetti mobile-390 e desktop-1440 / media.ts:16 «Scelta cliente (2026-08-03)» sta solo in un commento del codice, non nel registro §11 (spec:335-360): attribuzione da verificare prima di citarla come direttiva C
ERRORI DI FATTO: Il Congedo andrebbe in pausa con "quick-look": nessun componente registra quell'overlay (CaseQuickLook.tsx non chiama setOverlay) e CaseQuickLook non è in home. / Il test «aprendo VideoLightbox dalla testimonianza, il Congedo in vista va in pausa» è impossibile: gli apritori sono solo Voci e FeaturedTestimonial, a più schermate dal Congedo (page.tsx:81-92), e con rootMargin 40 % la banda è già in pausa. / currentState dice che il grep di test e scripts «non trova congedo»: app/lib/__tests__/intro-clocks.test.ts:119 e :214 contengono «congedo», ma riferito al congedo del lockup del preloader. Nella sostanza resta vero che nessun test copre il gate del Congedo. / currentState: «il moov sta al byte 36». Il box moov comincia al byte 32 (campo size), al 36 c'è il tag. Faststart confermato, differenza irrilevante.
### P2 — Asset della clip drone: ricodifica leggera, webm e variante 1280 (nessun gesto) [adopt-with-conditions]
kind=tokens-or-infra sticky=False files=public/media/domus-hero.mp4, public/media/domus-hero.webm (nuovo), public/media/domus-hero-720.mp4 (nuovo), app/lib/media.ts, app/components/Congedo.tsx dir=docs/hero-video.md:40-48 (ricetta di compressione),PRODUCT.md:166 (una foto vera in un posto solo: la clip resta una),D15, spec:429 (scala 1.14 del Congedo)
ADATTAMENTO: Guadagno di prestazioni sull'unica clip vera. Nessuna clip nuova, niente stock, niente fiori.

Oggi domus-hero.mp4 è 4,0 s, 100 fotogrammi, 1920x1080, 10,8 Mbps, 5,4 MB, faststart già a posto (moov al byte 36), con una traccia dati inutile. Doc: target «< 4-6 MB», ricetta in docs/hero-video.md:40-48.

Passi:
1) mp4 H.264 `-an -map 0:v:0 -c:v libx264 -crf 24..26 -preset slow -movflags +faststart` sugli STESSI 100 fotogrammi. Nessun ritaglio del loop, così la cucitura della ripetizione non cambia. La traccia dati sparisce.
2) Codifica VP9 (`-c:v libvpx-vp9 -crf 34 -b:v 0`) in /media/domus-hero.webm e valorizzare `heroCinematic.webm` (media.ts:20-22). Congedo.tsx:122 e HeroCinematic.tsx:442 la mettono già prima dell'mp4 quando la stringa non è vuota.
3) Variante 1280x720 con `<source media="(max-width: 1099.98px)">` prima delle sorgenti 1080p. La soglia vale 1100 e non 1280 per la scala 1.14 del Congedo (Congedo.tsx:105, :115): a 1100 px di banda la resa è 1254 px, sotto i 1280 della sorgente. Più in su servirebbe la 1080p, che in realtà viene già ingrandita di 1,14 a 1920 di viewport.

Il video parte solo da 768 con motion ok e mai con saveData (P1): una variante per telefono non serve. Reduced-motion invariato, nessun byte.
RISCHI: La ricodifica non toglie il logo impresso: quello resta un file sorgente da chiedere alla cliente, non un lavoro di encoder. / Artefatti di banding sul cielo e sull'acqua della piscina a CRF alto: confrontare a vista i fotogrammi 0, 50 e 99 prima di pubblicare. / Turbopack e la cache di .next servono asset vecchi (memoria domus-wow-layer): verificare con rm -rf .next dopo aver sostituito i file.
TEST: Nessun test esistente legge il file. / Controllo manuale: DevTools a 1024 e a 1440, verificare quale source viene scelta e il peso della richiesta.
VERDETTO: Lavoro di infrastruttura, invisibile: nessun gesto, nessuna sticky, nessuna clip nuova. I fatti sulla clip tornano con ffprobe (4,0 s, 100 fotogrammi a 25 fps, 1920x1080 H.264, 10,8 Mbps, 5.404.642 byte, una traccia dati) e il faststart c'è. Il codice rende già la sorgente webm prima dell'mp4 quando la stringa non è vuota (Congedo.tsx:122, HeroCinematic.tsx:442).

Il conto della variante 720 però è sbagliato. Ignora min-h-[70svh] della banda (Congedo.tsx:85): su un tablet verticale 768x1024 la scatola è 768x717, il cover rende 1275 px, per 1,14 fa 1453, e la 720 viene ingrandita. Ignora anche il DPR.

C'è poi il media di <source> dentro <video>: rispettato solo dai browser recenti (Chrome e Firefox 120+). I più vecchi prendono la prima sorgente, cioè la 720 per tutti.

La ricodifica parte da un H.264 già compresso, non dal master: perdita di generazione, e il rischio banding su cielo e acqua va verificato a vista. Era toglie le sorgenti webm su Safari (main.pretty.js:737-739), ma per clip con alfa: qui non vale, però iPad Safari (>= 768 riproduce) va provato. Il 2026-09-13 il file resta consumato solo dal Congedo.
CONDIZIONI: Passi 1 e 2 (mp4 ricodificato sugli stessi 100 fotogrammi senza traccia dati, più webm VP9) ammessi dopo un confronto a vista dei fotogrammi 0, 50 e 99 e del raccordo del loop contro l'originale, che resta nella storia git. / Variante 720 sospesa finché il conto non si rifà con la scatola vera (max di W×9/16 e 70svh), la scala 1.14 e il DPR, oppure si toglie. Non basta la soglia 1100 calcolata sul solo 16:9. / Se la variante resta, dichiarare nel commento che il media su <source> di <video> è ignorato dai browser vecchi, e allineare il nome alla ricetta già scritta in HeroCinematic.tsx:349 (hero-mobile.mp4, <= 3 MB, 720p) invece di inventarne un secondo. / Provare Safari su iPad (>= 768) con la sorgente webm per prima; se la riproduzione dà problemi, mp4 prima di webm. / Dopo la sostituzione dei file: kill del server e rm -rf .next (memoria domus-wow-layer, cache Turbopack), poi controllare in DevTools la sorgente scelta e il peso a 1024 e a 1440. / Il gate saveData citato («mai con saveData») vale solo se P1 atterra con quella condizione.
EVIDENZE: ffprobe public/media/domus-hero.mp4: h264 1920x1080 25/1, 100 fotogrammi, 4.000 s, 10.803.634 bps; traccia data; size 5.404.642 / xxd: ftyp a 0, box moov a 32 (tag a 36): faststart / media.ts:20-22 webm vuoto non renderizzato / Congedo.tsx:85 aspect-video min-h-[70svh]; :105 e :115 scale-[1.14] origin-bottom-right / docs/hero-video.md:44-51 ricetta libx264 crf 24 e VP9 crf 34 / HeroCinematic.tsx:347-353 ricetta per hero-mobile.mp4 <= 3 MB 720p / reverse-engineering/era-residence/js/main.pretty.js:737-739 rimozione di source[src$=.webm] su Safari; README.md:441-444 webm VP9+alfa e mov HEVC+alfa
ERRORI DI FATTO: La soglia <source media="(max-width: 1099.98px)"> è calcolata su una banda sempre 16:9: la banda ha min-h-[70svh] (Congedo.tsx:85), quindi dove 70svh supera W×9/16 la resa supera i 1280 px già sotto i 1100 di viewport (768x1024: circa 1453 px). / «Il video parte solo da 768 con motion ok e mai con saveData»: oggi il gate saveData non esiste (Congedo.tsx:56-64). Vale solo dopo P1.
### P3 — Linguaggio di tempo condiviso per le aperture: token media in gsap.ts e sipario del territorio deduplicato [adopt-with-conditions]
kind=tokens-or-infra sticky=False files=app/lib/motion/gsap.ts, app/components/motion/HorizonScroller.tsx dir=A17, spec:406 (perimetro era-residence §11.1-11.2: la tendina .img.loc-path, README.md:426),D04, spec:418 (sizes = pixel resi),DESIGN.md:535 (meccanica di «Perché Domus Tua»)
ADATTAMENTO: RIFINITURA dell'unica apertura a clip esistente, il sipario del territorio, senza cambiarne i valori.

1) In app/lib/motion/gsap.ts, accanto a `dur`/`stagger`/`dist` (gsap.ts:85-109), aggiungere `export const media = { curtain: { dur: 1.6, ease: "dtOut", from: "inset(0% 100% 0% 0%)", scaleFrom: 1.15 } } as const`. Sono i valori di oggi (HorizonScroller.tsx:62, :340-354, :582-595). Il vocabolario di P4, se approvato, entra qui e non come numeri sparsi: è il «pugno di ease e durate con nome» dell'articolo, fatto con le ease che il sito ha già (dtOut, domus.inOut).

2) In HorizonScroller.tsx una sola funzione `armCurtain(el, trigger)` al posto delle due copie (:334-360 in colonna con start "top 70%", :580-601 da lg con containerAnimation e start "left 90%"). Restano identici restart() all'ingresso, reverse() su onLeaveBack (l'uscita speculare) e la guardia `getClientRects()`. will-change: clip-path solo tra onStart e onComplete/onReverseComplete, lo schema di globals.css:1994-2001; oggi il sipario non lo promuove.

3) Verifica della regola dei sizes sulla scala 1.15, conto da rifare con `npm run probe:crop`. A 1440 la colonna media del pannello misura circa 700 px; con la sorgente 2:1 in una scatola 16:9 la resa è circa 787 px, contro `sizes` 55vw = 792 (HorizonStory.tsx:312). A DPR 1 il loader sceglie la variante da 1024 (next.config.ts:149) e 787×1,15 = 905 < 1024; a DPR 2 prende la 1920 e 1811 < 1920. Il transitorio non ingrandisce oltre la variante: 1.15 può restare.

Nessun cambio visibile, nessun gesto nuovo. Reduced-motion e colonna senza JS invariati: gli stati li scrive solo JS dentro matchMedia.
RISCHI: La memoria segnala che useGSAP con dependencies non riverte senza revertOnUpdate: il refactor non deve cambiare il ciclo di vita del context di HorizonScroller (refreshKey = locale). / Il ramo containerAnimation e il ramo verticale hanno inneschi diversi per ragioni misurate (HorizonScroller.tsx:316-333): la funzione condivisa deve ricevere il trigger dall'esterno, non unificarlo.
TEST: Nessun test legge data-horizon-slide o CURTAIN_DUR (grep su e2e/, app/lib/__tests__, scripts/). / Controllo manuale del sipario a 1440 e a 390 con motion ok, andata e ritorno.
VERDETTO: Refactor senza cambio visibile dell'unica apertura a clip esistente, coerente con la regola di gsap.ts:8 («Tutta la coreografia usa SOLO questo vocabolario»). I valori citati sono esatti:
- CURTAIN_DUR 1.6 (HorizonScroller.tsx:62);
- inset(0% 100% 0% 0%) e scala 1.15 in entrambi i rami (:340-341, :582-583);
- start "top 70%" in colonna (:345) e containerAnimation con "left 90%" da lg (:589-590);
- restart all'ingresso e reverse su onLeaveBack (:356-358, :597-599).

Il conto dei sizes torna: nel pannello del territorio la griglia 46fr/54fr dentro max-w 1600 con px 5vw dà una colonna di circa 700 px a 1440. Con la sorgente 2:1 la resa è circa 787 px; sizes 55vw = 792; il loader sceglie 1024 a DPR 1 (next.config.ts:149) e 787×1,15 = 905 regge.

Imprecisioni:
- la guardia getClientRects() esiste solo nel ramo in colonna (:338), non da lg (:580-583);
- il rischio revertOnUpdate è già coperto: HorizonScroller passa revertOnUpdate: true (:641).

Il will-change a tempo è un piccolo cambio di prestazioni, non di aspetto. Il valore del lavoro è basso ma sicuro.
CONDIZIONI: Il trigger (start e containerAnimation) arriva dall'esterno ad armCurtain: i due rami restano con inneschi diversi, come misurato in HorizonScroller.tsx:316-333. / Non chiamare l'export `media` in gsap.ts (si confonde con app/lib/media.ts): per esempio `curtain`. / Il vocabolario di P4 non entra in gsap.ts finché Alberto non approva P4. / Portare la guardia getClientRects anche nel ramo lg è ammesso solo se non cambia i trigger della polaroid hidden lg:block: verificarlo, non darlo per identico. / Il will-change: clip-path a tempo si pulisce nel cleanup del context, non con gsap.set dentro un callback (lo schema di Parallax.tsx:105-122). / Controllo manuale andata e ritorno a 1440 e a 390 con motion ok, e un attraversamento di 1024 con la finestra ridimensionata (revert di matchMedia).
EVIDENZE: HorizonScroller.tsx:62 CURTAIN_DUR = 1.6 / HorizonScroller.tsx:334-360 ramo in colonna, start "top 70%", guardia getClientRects a :338 / HorizonScroller.tsx:580-601 ramo lg, containerAnimation: tween, start "left 90%", nessuna guardia getClientRects / HorizonScroller.tsx:641 dependencies [refreshKey], revertOnUpdate: true / HorizonStory.tsx:266 griglia lg:grid-cols-[minmax(0,46fr)_minmax(0,54fr)] max-w-[1600px] px-[5vw]; :312 sizes "(min-width: 1024px) 55vw, 100vw" / gsap.ts:85-109 dur/stagger/dist; :73 dtOut / next.config.ts:149 deviceSizes [360, 420, 640, 768, 1024, 1280, 1536, 1920, 2560] / globals.css:1994-2001 will-change clip-path a tempo sulle stelle / grep di data-horizon-slide e CURTAIN_DUR in e2e/ e app/lib/__tests__: nessun test
ERRORI DI FATTO: «Restano identici … la guardia getClientRects()»: la guardia esiste solo nel ramo in colonna (HorizonScroller.tsx:338), non nel ramo lg (:580-583). / Il rischio «useGSAP con dependencies non riverte senza revertOnUpdate» è già risolto in questo componente: HorizonScroller.tsx:641 passa revertOnUpdate: true.
### P4 — Vocabolario di tre aperture RETTANGOLARI per le foto di capitolo (salita, dalla piega, otturatore) [needs-alberto]
kind=new-gesture sticky=False files=app/components/motion/MediaReveal.tsx (nuovo), app/lib/motion/gsap.ts, app/components/Posizionamento.tsx, app/components/Paths.tsx, app/components/Method.tsx dir=C03, spec:367 (eliminare tante animazioni: superata in parte da A12 solo per i set piece),A12, spec:401,A17, spec:406 (perimetro dichiarato: §7 = testi per righe, §11.1-11.2 = scroller; le aperture sulle foto di capitolo stanno FUORI dal perimetro scritto),DESIGN.md:573 e PRODUCT.md:132 (tre gesti),PRODUCT.md:128 (bello anche fermo),D04, spec:418 (sizes),DESIGN.md:421 (linguaggio squadrato), DESIGN.md:581 (niente card),DESIGN.md:593 (1,43)
ADATTAMENTO: GESTO NUOVO, fuori dai «tre gesti» (DESIGN.md:573, PRODUCT.md:132): va approvato da Alberto prima di scriverlo (vedi rischi). Nessuna sezione sticky, nessun pin.

Primitiva app/components/motion/MediaReveal.tsx, che avvolge la scatola del modulo:
- prop `geometry: "salita" | "piega-sx" | "piega-dx" | "otturatore"`;
- prop `scaleFrom` (default 1.06; 1 = solo clip);
- tutto dentro `gsap.matchMedia().add(MQ.motionOk)`, a ogni larghezza come il sipario (HorizonScroller.tsx:53-62);
- stato iniziale scritto SOLO da JS con fromTo (mai from);
- innesco con IntersectionObserver e non ScrollTrigger, perché sotto le runway lunghe della home ScrollTrigger è inaffidabile (memoria domus-motion-architecture). `rootMargin: "0px 0px -30% 0px"`, cioè l'equivalente di "top 70%", la misura presa per il sipario in colonna (HorizonScroller.tsx:322-326);
- ingresso dal basso: `tl.restart()`; uscita verso il basso: `tl.reverse()`, la chiusura speculare con la stessa geometria, come Era a durata piena (main.pretty.js:646-662) e come il sipario (HorizonScroller.tsx:358); uscita in alto: niente, resta aperta;
- rete a 2500 ms, con guardia `if (!entrata)`: `tl.progress(1)` (schema di Reveal.tsx:44);
- clip-path sul nodo `.dt-media-half`, contro-scala sull'`<img>` di next/image, MAI sull'inner di Parallax, la cui transform è di GSAP;
- will-change: clip-path solo durante il tween.

GEOMETRIE, tutte rettangoli a filo; al primo e all'ultimo fotogramma la foto è il modulo intero:
- SALITA, `inset(100% 0% 0% 0%)` → `inset(0%)`, dur.hero 1,4 s, ease dtOut. È l'«initial» di Era (main.pretty.js:665) e lo stesso verbo di TextLines. Capitolo: Posizionamento (Posizionamento.tsx:83-98).
- DALLA PIEGA, tendina dritta che parte dalla mezzeria (il bordo interno del modulo cade a x=720 su 1440, memoria rivista-bianca). Foto a sinistra `inset(0% 0% 0% 100%)` → `inset(0%)`; foto a destra (`lg:order-2`) `inset(0% 100% 0% 0%)` → `inset(0%)`; sotto lg, in colonna, entrambe da sinistra. 1,2 s (durL di Era), ease dtOut. Capitolo: Paths, due righe alterne (Paths.tsx:236-247).
- OTTURATORE, `inset(0% 50% 0% 50%)` → `inset(0%)`, dur.reveal 0,9 s, ease "domus.inOut" (gsap.ts:67). Capitolo: Method, i tre atti (Method.tsx:225-236).
Il sipario del territorio (1,6 s, dtOut, da sinistra) resta il quarto e più lento. Nessuna coppia condivide durata ed ease.

CONTRO-SCALA DENTRO LA REGOLA DEI SIZES (D04, DESIGN.md:499): 1.06 → 1 solo se la sorgente è larga almeno 2 × 1,06 × la larghezza resa (DPR 2), altrimenti `scaleFrom={1}`.
- Posizionamento: consulenza.jpg 1920 px, resa circa 715 px a 1440, servono 1516: ok. Anche la variante del loader regge, 768 a DPR 1 contro 758 resi; con 1.15 sarebbero 822 e non reggerebbe.
- Method: il ritratto (763 px, Method.tsx:173) e villa-mozart (1280 px resa 862) → scala 1; handshake (1920 px, resa 605) → 1.06.
- Paths: conto da rifare con `coverSizes(p.ratio)` (Paths.tsx:48).
Mai oltre 1.43 (DESIGN.md:593).

NIENTE poligono diagonale (vedi excluded), niente blur (DESIGN.md:417, :580).

ESCLUSI DAL GESTO: facciate e link (Voci, FeaturedTestimonial, OpenDomus, Short, /lavora-con-noi), foto LCP (banda hero, PageHero), rotaia e stelle (hanno già il loro), Services (render di repertorio, PRODUCT.md:167-170), pagine interne.

Reduced-motion e no-JS: foto intera, nessuno stato scritto. CLS 0: clip-path e transform non toccano il layout.
RISCHI: È un quarto gesto: contraddice alla lettera DESIGN.md:573 e C03 (la cliente). A12 e A17 sono di Alberto e non coprono le foto di capitolo; la memoria di progetto chiede «il riferimento visivo ce l'ha?», e goldengoal no (le sue animazioni sono split per righe, fadeInUp, parallasse e slider), era-residence sì (slide, loc-path). Serve il sì di Alberto, e lui decide se sentire la cliente. Senza, si ferma a P3. / Non è una curva né una card: al riposo resta un rettangolo a filo senza raggio, ombra o cornice. Ma una foto che sparisce a metà quando si risale è proprio il tipo di «animazione in più» che la cliente ha tolto: tenerla su tre capitoli, non su tutte le foto. / Tre aperture più la deriva Parallax sullo stesso nodo: verificare a 390 che il paint del clip non si sommi allo scrub (un clip su una foto sola per vista). / La contro-scala su img di next/image: `fill` mette l'img in absolute, e una transform lì non deve toccare `objectPosition` (Method.tsx:233). / Se il nuovo CSS (will-change) finisce in globals.css prima di `.dt-media-half,`, moduli-media.test.ts:57-63 fa slice per indexOf e si rompe: metterlo dopo il blocco dei moduli (dopo globals.css:436).
TEST: app/lib/__tests__/moduli-media.test.ts (ordine dei selettori in globals.css) / e2e/motion.spec.ts:115+ (parallasse su /vendi: legge la matrice dell'inner di Parallax risalendo dall'img. PageHero è escluso, ma la contro-scala sull'img non deve essere presa per la matrice cercata) / Nuovo e2e in e2e/motion.spec.ts: con motion ok, prima dell'ingresso `clip-path` diverso da none sulla scatola di Posizionamento, dopo lo scroll `inset(0%)`; con reducedMotion reduce `clip-path: none`; a 390 la foto è visibile a fine tween.
VERDETTO: È un quarto gesto. Contraddice alla lettera DESIGN.md:573 («limitare il movimento ai tre gesti»), PRODUCT.md:132 e C03 della cliente (spec:367). A12 (spec:401) copre solo i set piece, e il perimetro di era-residence scritto in A17 (spec:406: §2, §4, §5, §7 «testi che salgono per righe», §11.1-11.2) non comprende le aperture sulle foto di capitolo. Il dossier elenca però lo slide proprio nella tabella degli animatori del suo §7 (README.md:331). Il test della memoria («il riferimento ce l'ha?») lo passa via era-residence, non via goldengoal.

La memoria di questa sessione (domus-coreografia-era.md, 2026-09-13) registra che Alberto ha scelto «coreografia piena» e «sticky dove serve» (A18, A19), ma il registro §11 si ferma ad A17: finché A18 non è scritto con le sue parole la proposta resta fuori dal registro.

Nel merito tecnico le basi reggono:
- solo clip-path e transform, CLS 0;
- stati scritti solo da JS dentro MQ.motionOk;
- IntersectionObserver invece di ScrollTrigger (la trappola in fondo alla home);
- nessuna foto LCP;
- conti dei sizes esatti per Posizionamento (consulenza 1920x1625, resa 715 px, variante 768 contro 758) e per Method (ritratto 763 senza scala, villa-mozart resa circa 864, handshake 605).

Due difetti vanno corretti prima di costruire. Primo, la rete a 2500 ms con guardia if (!entrata) → tl.progress(1): ogni foto sotto la piega non è ancora entrata a 2,5 s, quindi viene aperta dalla rete. Al primo vero ingresso restart() la richiude di colpo e la riapre: un salto visibile, e il gesto non si vede mai pulito. Secondo, P4 sostiene che l'uscita speculare sia «come Era», ma in Era lo slide a scroll è once: true (main.pretty.js:915): nessuna chiusura risalendo. La chiusura speculare esiste solo nel sipario Domus (HorizonScroller.tsx:358).
CONDIZIONI: Da chiedere ad Alberto, testualmente: «Le aperture a clip rettangolari su tre foto di capitolo (Posizionamento, Paths, Method), che si richiudono solo risalendo, rientrano nella tua scelta del 13 settembre "coreografia piena"? Posso scriverla nel registro §11 come A18 con queste tue parole, e dichiarare in DESIGN.md:573 un quarto gesto? La mostri tu alla cliente, visto che va contro "eliminare tante animazioni e transizioni" (C03)?» / Prima di scrivere codice: A18 (e A19 se serve) nel registro §11 con parole esatte e fonte, mai attribuiti alla cliente. / La rete di sicurezza scatta solo se l'elemento è geometricamente in viewport a 2,5 s e l'IO non ha ancora risposto; mai progress(1) su foto sotto la piega. In alternativa lo stato chiuso si scrive al primo callback IO con isIntersecting false, non al mount. / Contro-scala massima 1.06 e solo dove la sorgente regge 2×1,06×resa (Posizionamento sì; ritratto e villa-mozart di Method a scale 1). Il riferimento corretto è DESIGN.md:397 (~1,05× di ingrandimento), non DESIGN.md:593. / Esclusi confermati: facciate e link, foto LCP, Services, rotaia, stelle, Congedo, pagine interne. Clip-path sul nodo .dt-media-half dentro l'inner di Parallax, contro-scala sull'img, mai sull'inner di Parallax. / CSS nuovo dopo globals.css:436, così lo slice per indexOf di moduli-media.test.ts:57-63 non si rompe. / Test e2e nuovi in e2e/motion.spec.ts con test.use reducedMotion no-preference per lo stato chiuso e aperto, reduced per clip-path none; a 390 la foto è intera a fine tween.
EVIDENZE: DESIGN.md:573 Do: tre gesti; DESIGN.md:587 Don't: nessuna sezione sticky oltre i tre nastri; PRODUCT.md:128 bello anche fermo; PRODUCT.md:132 tre gesti / spec:367 C03; spec:401 A12; spec:406 A17 con perimetro §2, §4, §5, §7 testi, §11.1-11.2; il registro si ferma ad A17 (spec:388-406) / memoria domus-coreografia-era.md (2026-09-13): Alberto sceglie «Coreografia piena» e «Sticky dove serve», da registrare come A18 e A19, contro C03 / reverse-engineering/era-residence/README.md:331 slide nella tabella degli animatori §7; main.pretty.js:909-917 ScrollTrigger once: !0; :1009-1024 slide raggruppato e armato con initial / main.pretty.js:626-644 reveal polygon più inner scale 1.5, xPercent 25, durL, InOut / HorizonScroller.tsx:356-358 restart e onLeaveBack reverse: l'uscita speculare esistente è Domus, non Era / Reveal.tsx:41-44 rete a 2500 ms senza guardia; memoria domus-wow-layer: i failsafe temporizzati vanno guardati o uccidono il replay / Posizionamento.tsx:80 consulenza 1920×1625; :93 sizes 50vw; Method.tsx:173-175 ratio 763/442, 1280/505, 1920/1087; Method.tsx:224 media a destra da lg per tutti e tre gli atti; Paths.tsx:236 lg:order-2 sulla riga pari / moduli-media.test.ts:57-63 slice per indexOf; e2e/motion.spec.ts:132-136 cerca la matrice risalendo dai genitori dell'img, non l'img stessa / DESIGN.md:397 «mai oltre ~1,05× di ingrandimento»; DESIGN.md:593 1,43 riferito solo alle copertine rifilate
ERRORI DI FATTO: «Uscita verso il basso: tl.reverse(), la chiusura speculare … come Era a durata piena (main.pretty.js:646-662)»: in Era il reveal slide a scroll è once: true (main.pretty.js:915) e l'animatore hide non è legato alla risalita. La chiusura speculare allo scroll esiste solo nel sipario Domus (HorizonScroller.tsx:358). / «Mai oltre 1.43 (DESIGN.md:593)» usato come tetto generale: DESIGN.md:593 riguarda il rifilo delle copertine video (D13). La regola generale d'ingrandimento è DESIGN.md:397 (~1,05×), che la contro-scala 1.06 supera di poco nel transitorio. / La rete «if (!entrata) tl.progress(1)» a 2500 ms, come descritta, apre tutte le foto sotto la piega prima che l'utente ci arrivi: non è lo «schema di Reveal.tsx:44» senza effetti collaterali, e produce un salto chiuso-aperto al primo restart().
### P5 — Documenti: regola dei video d'ambiente, vocabolario delle aperture (se approvato), commento stantio di Reveal [adopt-with-conditions]
kind=docs-only sticky=False files=DESIGN.md, PRODUCT.md, docs/superpowers/specs/2026-09-10-redesign-rivista-bianca-design.md, docs/hero-video.md, app/components/Reveal.tsx, app/globals.css, .impeccable/design.json dir=spec §11 (unico registro, spec:335-360),DESIGN.md:573,PRODUCT.md:132,PRODUCT.md:166
ADATTAMENTO: 1) DESIGN.md, Components, dopo «Banda video di congedo» (DESIGN.md:559-560): nuova voce «Video d'ambiente».
- Una sola clip vera (domus-hero.mp4, 4 s) e una volta per pagina (PRODUCT.md:166, spec:432).
- Nessun autoPlay: il gate lo decide useAmbientVideo (IO a 40 %, motion ok, 768 px, niente saveData, pausa sotto VideoLightbox).
- Ripresa dal punto di pausa.
- Le facciate YouTube non si mettono mai in pausa da sole (LazyYouTubeEmbed.tsx:30-34).

2) Se Alberto approva P4:
- tabella «Aperture delle foto» in DESIGN.md (geometria, capitolo, durata, ease, scala, esclusi);
- Do di DESIGN.md:573 e Motion di PRODUCT.md:132 riscritti come «quattro gesti», con richiamo alla nuova voce A del registro §11.1 (spec:401-406), che dica chi l'ha chiesto e con quali parole;
- aggiornare .impeccable/design.json.

3) Correggere il docstring stantio di Reveal.tsx:15 («fade-up + de-blur» → «fade-up di 2.5rem, senza blur»): la CSS non ha blur (globals.css:508-521). Togliere i `filter: none` orfani in globals.css:530 e :538.

4) docs/hero-video.md: aggiungere al banner le misure vere della clip (4,0 s, 10,8 Mbps, faststart) e, dopo P2, i file webm e 720.
RISCHI: Il registro §11 è l'unica fonte di chi-ha-detto-cosa: la voce del nuovo gesto va attribuita ad Alberto solo con le sue parole esatte, mai alla cliente (memoria rivista-bianca, «Attenzione alle attribuzioni»). / Toccare globals.css per i filter orfani: con Turbopack la CSS servita è di un'edizione indietro (memoria), quindi verificare dopo rm -rf .next.
TEST: app/lib/__tests__/moduli-media.test.ts (se si tocca globals.css vicino ai moduli)
VERDETTO: Solo documenti e pulizia. Il docstring di Reveal.tsx:15 dice davvero «fade-up + de-blur» mentre la CSS non ha blur (globals.css:508-521). I filter: none orfani sono in globals.css:530 e :538. Si possono correggere subito, senza effetti visibili.

Il resto non va scritto prima del codice. DESIGN.md dichiara di essere «scritto dal costruito» e che «dove questo file e il codice non coincidono vale il codice» (DESIGN.md:298). La voce «Video d'ambiente» descrive un hook e una pausa sotto VideoLightbox che P1 non deve introdurre (vedi P1), quindi va scritta dopo P1 e con quello che P1 costruisce davvero. La tabella delle aperture e la riscrittura «quattro gesti» di DESIGN.md:573 e PRODUCT.md:132 dipendono dal sì di Alberto su P4 e dall'iscrizione di A18 nel registro §11, oggi assente. La nota sul registro unico (spec:337-353) e sulle attribuzioni è corretta.
CONDIZIONI: Punto 3 subito: docstring di Reveal.tsx:15 corretto e filter: none tolti da globals.css:530 e :538. Dopo il tocco a globals.css: kill del server, rm -rf .next, verifica (memoria domus-wow-layer). / Punto 1 solo dopo P1 e con il comportamento costruito: IO 40 %, motion ok, 768, saveData e visibilitychange se entrano. Niente «pausa sotto VideoLightbox» se P1 la toglie. Estendere a «una clip per pagina» la regola di PRODUCT.md:166 dichiarandola decisione di lavoro, non direttiva. / Punto 2 solo dopo il sì di Alberto su P4 e dopo aver scritto A18 nel registro §11 con le sue parole esatte e la fonte. Aggiornare insieme DESIGN.md, PRODUCT.md e .impeccable/design.json. / Punto 4 (docs/hero-video.md) solo con i numeri misurati dopo P2, se P2 atterra. / Non chiamare «direttiva della cliente» la scelta del 2026-08-03 su enabled: false: sta solo in un commento (media.ts:16) e non nel registro §11.
EVIDENZE: Reveal.tsx:15 «fade-up + de-blur» / globals.css:508-521 .reveal senza filter; :530 e :538 filter: none orfani / DESIGN.md:298 scritto dal costruito, vale il codice; DESIGN.md:559-560 Banda video di congedo / DESIGN.md:573 e PRODUCT.md:132 tre gesti / spec:337-353 registro unico; spec:388-406 Alberto fino ad A17 / memoria domus-coreografia-era.md: A18 e A19 da registrare / memoria domus-redesign-rivista-bianca.md: «Attenzione alle attribuzioni» / .impeccable/design.json presente
ERRORI DI FATTO: La voce proposta dice «pausa sotto VideoLightbox» come parte del gate: in pratica non ha casi (apritori solo in Voci.tsx:310 e FeaturedTestimonial.tsx:178, lontani dal Congedo in page.tsx:81-92).
ESCLUSO: Nuove clip d'ambiente come texture in altre sezioni (fiori, acqua, vegetazione, stock) — In public/ esiste una sola clip vera, public/media/domus-hero.mp4, già usata dal Congedo. I fiori video con alfa sono proprio la texture di Era (README.md:437-457) e sono vietati. Filmati di repertorio contraddicono «niente render di riempimento» (PRODUCT.md:167-170). Riusare la clip drone in un'altra sezione la farebbe comparire due volte nella pagina. (C12, spec:376 (niente fiori); decisione di lavoro «una fotografia compare una volta per pagina» (spec:432, PRODUCT.md:166); PRODUCT.md:167-170)
ESCLUSO: Riaccendere il video nell'hero come texture d'apertura — `enabled: false` è attribuito alla cliente (media.ts:16-18, «Scelta cliente (2026-08-03)»): solo lei può rimetterlo. La stessa ripresa drone aprirebbe e chiuderebbe la home, cosa che il Congedo ha già evitato cambiando poster (Congedo.tsx:13-19). Manca l'asset mobile (HeroCinematic.tsx:347-353). La banda è il candidato LCP ed è legata al patto della porta. (Scelta cliente 2026-08-03 (media.ts:16-18); spec:432 (una foto per pagina); D06, spec:420 (patto della porta, test intro-clocks))
ESCLUSO: Il parallelogramma di animateSlide (polygon a 125 %, inner scale 1.5 + xPercent 25) — Un poligono ad angolo NON è una curva, non è blur e non è una card: nessuna di queste regole lo vieta. Lo escludono la scala interna 1.5, che rende il 50 % di pixel in più di quelli dichiarati in `sizes` (foto molle per mezzo tween InOut) e supera il tetto d'ingrandimento dichiarato di 1,43, e il bordo diagonale, che introduce una terza grammatica di forma dopo rettangoli e stelle in un linguaggio dichiarato squadrato. D04 è una decisione di lavoro e si potrebbe rivedere, ma costerebbe varianti d'immagine grandi 1,5 volte. P4 tiene la tecnica (clip più contro-scala) con geometrie rettangolari e scala 1.06. (D04, spec:418 e DESIGN.md:499 (sizes = pixel resi); DESIGN.md:593 (mai oltre 1,43); DESIGN.md:421 (linguaggio squadrato, da C01, spec:365))
ESCLUSO: Pausa automatica fuori schermo dei video YouTube scelti dall'utente (LazyYouTubeEmbed, VideoLightbox) — Non sono clip d'ambiente: hanno audio e li ha fatti partire la persona. Metterli in pausa allo scroll richiederebbe `enablejsapi=1` e postMessage, cioè più superficie YouTube, contro il click-to-load documentato. Il lightbox smonta già l'iframe alla chiusura (VideoLightbox.tsx:93). (Decisione di privacy documentata: LazyYouTubeEmbed.tsx:30-34, VideoLightbox.tsx:17-25 (docs/legal-launch-inventory.md))
ESCLUSO: Aperture a clip o a scala su facciate video e copertine rifilate (Voci, FeaturedTestimonial, OpenDomus, Short di HorizonStory, /lavora-con-noi) — Il bersaglio del play diventerebbe mobile o ritagliato (OpenDomus.tsx:155-156); un link tagliato a zero resta focusabile ma col focus invisibile; la regola delle card cliccabili ammette solo opacity, mai transform (memoria domus-wow-layer). Una contro-scala 1.06 sopra `.dt-still-trim` (1.43) arriverebbe a 1,52, oltre il tetto. (DESIGN.md:593 e D13, spec:427 (copertine rifilate, massimo 1,43); gsap.ts:20-25 (stati nascosti con link))
ESCLUSO: Aperture sulle foto LCP (banda dell'hero, PageHero delle pagine interne) — L'immagine hero e l'H1 non si nascondono mai. PageHero porta `preload` perché è l'LCP della pagina (PageHero.tsx:119-120). La banda dell'hero deve coincidere al pixel con la sagoma del preloader. (Regola LCP (hero image + H1 mai nascosti, memoria domus-motion-architecture); D06, spec:420 (patto della porta))
ESCLUSO: Sipario o otturatore sulla banda video di congedo — Il Congedo è dichiaratamente senza GSAP (Congedo.tsx:11). La sua scala 1.14 esiste solo per mangiare il logo impresso (Congedo.tsx:110-113) e consuma già il margine d'ingrandimento. Il titolo d1 bianco sopra il video sparirebbe a metà gesto. Sarebbe un altro gesto nell'ultima vista della pagina. (C03, spec:367 (eliminare tante animazioni); D15, spec:429 (Congedo: INK_ON_VIDEO, poster, scala 1.14))
ESCLUSO: Scala «da leggermente ingrandita a riposo» come Ken Burns in scrub su tutte le foto (img-in di Era, yPercent -20 → 0 con overscan) — Una deriva di -20 % richiede una foto più alta della scatola, cioè un ingrandimento permanente oltre i pixel dichiarati. È un secondo effetto legato allo scroll sulle stesse foto che hanno già Parallax (±0,56 %, DESIGN.md:508). La fotografia ferma non sarebbe più a riposo. (D04, spec:418 (sizes); PRODUCT.md:128 (bello anche fermo); C03, spec:367)
ESCLUSO: Rivelazioni con blur o de-blur (la texture reveal classica) — Nessuna sfocatura sulle foto né nei reveal: Reveal è già stato ripulito dal blur (globals.css:508-521) e resta solo un commento da correggere (P5). (C14, spec:378; DESIGN.md:417 (regola del piatto) e DESIGN.md:580)
### reinstate:Il parallelogramma di animateSlide (polygon a 125 %, inner scale 1.5 + xPercent 25) [needs-alberto]: L'esclusione mescola una ragione valida con due citazioni che non coprono il caso. La scala interna 1.5 cade davvero: D04 (spec:418, DESIGN.md:499) chiede sizes pari ai pixel resi, e DESIGN.md:397 fissa circa 1,05× di ingrandimento. Il bordo diagonale invece non lo vieta nessuna direttiva. C01 e DESIGN.md:421 vietano il curvo, e un bordo obliquo a metà transizione non è una curva; a riposo la foto torna rettangolo a filo. DESIGN.md:593 (1,43) riguarda solo il rifilo delle copertine (D13), non è un tetto generale. «Terza grammatica di forma» è un giudizio di gusto, quindi al massimo una decisione di lavoro, non una direttiva. Tolta la scala 1.5, la geometria a parallelogramma con contro-scala dentro il budget dei sizes è ammissibile quanto le geometrie rettangolari di P4, e quindi sta nella stessa domanda da fare ad Alberto: non va esclusa per direttiva. | Rimetterla fra le opzioni di P4, non come proposta autonoma: stessa domanda ad Alberto (A18, coreografia piena), stesse regole (IO, stati da JS, motion ok, foto non LCP). / Scala interna al massimo 1.06, e solo dove la sorgente regge 2×1,06×resa; niente xPercent 25, che scopre i bordi della foto dentro la scatola. / Se Alberto preferisce il linguaggio solo rettangolare, registrarlo come sua direttiva o come decisione di lavoro con la ragione scritta, non come conseguenza di C01.
