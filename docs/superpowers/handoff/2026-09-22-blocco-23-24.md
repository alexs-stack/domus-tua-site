# Handoff: blocco 23 (rifinitura) e blocco 24 (A46, il cielo è la pagina)

2026-09-22, 02:00 · branch `claude/rivista-bianca` a `c65e44f` · PR #79 aperta, non mergiata.

Scritto dalla sessione del 21-22 settembre sul PC fisso, per continuare su un altro PC. Il prompt da incollare è in `2026-09-22-prompt-sviluppo.md`, accanto a questo file. La memoria di progetto di Claude Code (`~/.claude/projects/…/memory/`) NON viaggia col repo: quello che serve sta qui.

## Cosa è stato fatto (tre commit, tutti spinti)

| Commit | Cosa | Verifica |
|---|---|---|
| `60e1f1c` | **Blocco 23, G1** — i cinque difetti visibili dell'audit del 21 settembre (Playwright headless sul build di `6e7fd80`, home + 11 rotte, 1440×900 e 390×844): V01 banner cookie che ricompariva (`afterCurtain` di `fold.ts`, callback che rilegge il cookie; `e2e/cookie-banner.spec.ts`); V04 titoli con una parola lunga fuori dallo schermo (`.text-d1`/`.text-d2` seguono la larghezza sotto i 480 px, `tightTitle` per lingua su PageHero, passi del Metodo a due colonne fra 768 e 1023, blocco morto della scala sotto i 1024 tolto; rete nuova per parola/titolo su 13 rotte × 5 lingue in `e2e/mobile-motion.spec.ts`); V05 salto di 192 px su /lavora-con-noi (`image-ratio.test.ts`); V06 «2.000 mq» in grassetto spezzato (`SURFACE_RE` in `lexicon.ts`); H03 lead del territorio sotto la foto aerea + refuso «di la provincia» (home, FAQ con JSON-LD, knowledge dell'assistente; `territorio-copy.test.ts`) | unit 2287/2288, tsc, lint; e2e cookie-banner + mobile-motion (parola) + home (territorio) 337/337 sui cinque viewport; tre revisori avversari + un giro di correzione |
| `1a0f8bf` | chore: `.claude/launch.json` con `domustua-start` (`next start -p 3199`) per le verifiche sul build | — |
| `c65e44f` | **Blocco 24, A46** — il cielo delle foto alte è trasparente e la pagina fa da cielo (vedi sotto) | unit 2300/2301, tsc, lint; e2e a28 + a11y + segno + home (finestra) 138/138 su mobile-390 e desktop-1440; corridors + pages + mobile-motion + motion 495/496 sui cinque progetti (l'unico rosso: sipario a sessione fredda su tablet-768, preesistente e legato al carico). **Revisione avversaria NON fatta** (i tre revisori erano appena partiti quando la sessione è stata chiusa per pushare): è il primo passo sull'altro PC |

### A46 in breve (parole di Alberto, 21 set. sera)

«su eraresidence questa foto che usa come background alta ha il cielo mascherato, è no bg: ecco perché sembra un tutt'uno il cielo con il colore dello sfondo del sito. Dobbiamo fare la stessa cosa nel nostro sito, dove ci sono le immagini così alte».

- `scripts/media/cielo.mjs` maschera il cielo per colore (giorno / sera / medio, flood fill dal bordo alto, tasche fra le foglie, alpha morbida al bordo) e scrive `public/images/reali/<nome>-cielo.webp` per le sette teste col cielo e la finestra di Open Domus; `tinte.mjs` scrive in `tinte.json` `cielo: { file, linea, cima }` e tinta alta «avorio». I due attici (interni) non hanno WebP. I JPEG restano di riserva.
- Le teste: riquadro = la carta (`--color-cream`), blocco dei testi in flusso sull'avorio, strato della foto sotto portato su di `cima` (dove il soggetto comincia), scritte nei colori della rivista (eyebrow rosso, h1 inchiostro, lead grafite, corsivo rosso, CTA rossa, link fantasma inchiostro). La deroga WCAG 1.4.3 delle teste (A38/A40, bianco nudo) è chiusa: `a11y.spec.ts` misura h1 e lead ≥ 4,5:1.
- Testata in inchiostro su ogni rotta (via `suFoto`/`data-su-foto` e D82). Finestra di Open Domus: WebP col cielo, titolo inchiostro come «ARCHITECTURE» su era, segno `foto-chiara`.
- Pellicole prima/dopo in `2026-09-22-pellicole/` (le altre stanno solo nello scratchpad del PC fisso).

## Foglio delle decisioni per Alberto

Ordinate: prima ciò che è visibile alla cliente. Ogni voce ha la prova nel repo o nelle pellicole.

1. **H01 — logo bruciato nella clip del Congedo** (`H01-congedo-logo-bruciato-1440.jpg`): a schermo intero e nello slot si vede il logo «DomusTua» cotto nella clip, sotto il monogramma. Prima di A35 lo mangiava la scala 1,14 dall'angolo basso-destro. **Pista A** (qui, senza master): token `--pc-crop: 1.14` applicato al DOM (poster + video, `transform-origin: 100% 100%`, `overflow: clip` sul contenitore) e allo shader della piega (`uv = 1 − (1 − uv)/u_crop` dopo il cover), stesso numero; test in `lastra.test.ts`, `congedo-cartolina.test.ts`, `e2e/home.spec.ts`; `docs/da-chiedere-alla-cliente.md` 6.5 riscritto (oggi chiede ancora una clip «muta» di «15-35 s», contro A29/A44). **Pista B** (sul portatile, col master 4K e ffmpeg): `crop` in `scripts/media/congedo.mjs`. La chiusura vera resta la clip senza logo dalla cliente. *Pronto da costruire: il prompt lo prevede come G2.*
2. **A46: `cima` e non `linea`** — il compito diceva blocco = max(100svh, `linea` × altezza); con la linea di /vendi (0,488) l'H1 posava sui cipressi, con la cima (0,219) il soggetto comincia esattamente al fondo del blocco (`dopo-A46-vendi-1440.jpg`). Va bene così? Se vuoi più villa nel primo schermo la leva è una foto con meno cielo, non la formula.
3. **A46: /open-domus e /recensioni** hanno `cima` 0 (la tenda e il muro bianco toccano il bordo alto): si comportano come interni, la foto comincia sotto i comandi. Foto di giorno di riserva, non usate: `villa-portico-glicine-alta.jpg`, `villa-ingresso-scala-alta.jpg`.
4. **A46: la finestra** — i vetri dei parapetti sono stati resi trasparenti (altrimenti restavano riquadri azzurri di cielo dentro il vetro, sulla carta); la variante coi vetri azzurri è stata scartata. Il segno vira `foto-chiara` sulla finestra.
5. **G1: la scala sotto i 480 px** — tutte le teste d1/d2 seguono la larghezza (a 390 una testa di capitolo è 31,2 px invece di 38,4; da 480 in su nulla cambia). L'alternativa (classe per componente) lasciava due taglie sulla stessa pagina; i revisori hanno preferito il rango unico. Se vuoi il 38,4 del riferimento anche a 390, si torna indietro in dieci minuti (`globals.css`, blocco `@media (max-width: 39.99rem)`).
6. **H02 — tessera «In studio» del team** (`H02-team-in-studio-tagliata-1440.jpg`): 3:2 nella cornice 4:5 taglia una persona a riposo sul desktop; contro A27 (18 set.) ma la cornice unica è una scelta dell'11 set. (`Team.tsx:99-105`, `team.ts:165`). Cornice 3:2 alla stessa altezza, altra foto in 4:5 dalla cliente, o si accetta?
7. **H05 — Open Domus a inizio corridoio** (`H05-open-domus-inizio-corridoio-1440.jpg`): a 1440 finché le tende non si aprono del titolo si vede una fetta. È lo stato 0 delle tende di era: voluto, o il titolo va tenuto sotto i fori?
8. **H10 — primo schermo della home senza H1 né CTA** (conseguenza di A44, banda 100svh): DESIGN.md dice ancora il contrario. Va bene così (come su era) o la CTA deve rientrare sopra la piega?
9. **H11 — «E MILANO» sulla foto aerea**: voluto (tecnica di era), ma la «O» sugli alberi scuri legge male e contraddice DESIGN.md:344. Eccezione dichiarata?
10. **Le cinque foto di Davide Salerno** (`villa-portico-tenda`, `villa-piscina-facciata`, `villa-fronte-acqua`, `villa-angolo-piscina`, `villa-lettini`, +`villa-vetrata-lanterne`, `piscina-lusso`): orfane da `6e7fd80`, ancora in `public/` e in `media-file.test.ts`; il punto 2.13 (licenza) resta BLOCCANTE per file che nessuna pagina rende. Si tolgono (e 2.13 si chiude) o restano di riserva?
11. **Commit 21 (strato transitions.dev)**: ancora voluto dopo A35-A45? Il codemod dei 30 frammenti corrisponde 30/30 sull'albero.
12. **Spec 13 set. §2.7**: sorgente del video d'ambiente scritta «al warm» (com'è) o «al primo play»?
13. **A28(10) Paths da sinistra, A32(5) collegamento fra le due righe di Paths, le X per foto della lama**: `docs/direttive-foto-entrata.md:160-164` le lascia ad Alberto.
14. **A31, A34, A37, A39**: le parole non sono nel repo (`progress.md`, `qualita/…` stanno sul portatile): servono per il registro.
15. **Base delle misure LCP** (Passo 22.6): `1a77744` (TextLines) o `aef9a7c`? E il permesso a `npx playwright install firefox webkit` per «trova nella pagina».
16. **Numerazione delle domande alla cliente**: la 27 è già occupata dalla piega del video; «più movimento di quello che avevate chiesto» diventa la 28, «le foto generate e le scritte» la 29.

## Revisione avversaria di A46 (22 set., sull'altro PC)

Quattro lenti in sola lettura sul build di e5e1663 (regole, codice, browser-geometria, browser-pixel: 30 rilievi) e due verificatori per rilievo (un confutatore con prove, un giudice delle regole). Esito: 19 confermati, 8 da portare ad Alberto, 2 respinti; un giro di correzione nel commit che segue e5e1663. Verificato e a posto sul build: geometria delle nove teste 45/45 (cinque viewport), CLS 0 su 45, click sui comandi 90/90, reduced-motion e senza JS identici, testata inchiostro senza `data-su-foto`, LCP = la foto (44-128 ms a caldo), contrasto h1/lead ≥ 4,5:1, nessun alone azzurro ai bordi del cielo (≤ 0,3 % dei pixel).

**Corretto** (commit successivo): i commenti di `testa.ts`, `PageHeroTesta.tsx` e `testa.test.ts` che descrivevano la geometria «linea» scartata (R01, R03, R10, C03); la riga A46 del registro §11 con l'hash e D186 viva (R02); `sizes` delle teste a `100vw` (lo strato è largo 100 % in flusso: 151vw faceva scaricare il bucket 1920 dove basta il 1280, +45…+72 KB AVIF sull'LCP del telefono; R04, C02, P01, G03); la prop `light` di LanguageSwitcher e la variante `ghost-dark` senza chiamanti (R05, C06, R09); il commento sull'alpha «lossless» (C05: nell'AVIF è lossy a q 38); la riga 40 della spec del 20 set. (R06); **il segno che spariva** (C01, G02: marcatore unico dalla cima in giù → tacche avorio sull'avorio del cielo e sui muri bianchi per 100-940 px di scroll): ora `tinte.mjs` misura per rotta le bande in cui la striscia del segno è opaca e scura (`segno: [[da, a], …]`) e `PageHeroTesta` rende un marcatore per banda; le tasche di cielo fra i cipressi di /vendi (P03: raggio della linea del cielo a 700 px e foschia azzurrina ammessa sopra la linea; restano una lama ridotta su /vendi, 665 px sorgente, e le due fra le fronde di /acquista, 1.695 e 783 px ≈ 5×5 px CSS a 1440, che stanno sotto la linea del cielo locale: un criterio per colonna è da fare).

**Respinti**: R11 (DESIGN.md «avorio profondo»: la deroga è già scritta altrove), C09 (la cima della finestra in un commento: la finestra cambia con A47).

**Foglio delle decisioni, voci nuove** (17-24):

17. **G01 / C08 — il buco d'avorio sotto i comandi**: `cima` è la prima riga con il 5 % di pixel opachi su TUTTA la larghezza, quindi la fa scattare una punta ai bordi; nella colonna centrale (30-70 %) il soggetto comincia più in basso e fra il link fantasma e la villa restano 0,77 schermi d'avorio su /metodo a 1440 (691 px), 0,64 su /recensioni, 0,44 su /domande-frequenti, 0,35 su /vendi; sul telefono 0,28 / 0,24 / 0,18 / 0,15 schermi. Misurate anche le cime al centro: /vendi 0,277 (json 0,219), /acquista 0,442 (0,402), /servizi 0,312, /metodo 0,339 (0,07), /recensioni 0,215 (0), /domande-frequenti 0,355 (0,267). Scelta di direzione: cima su tutta la larghezza (oggi), cima al centro, o linea. Con A48 (le scritte durante la foto) cambia comunque.
18. **C07 — /open-domus**: il segnaposto prima del decode è la carta ma la prima riga della foto è la tenda (#cfbeb2): sul telefono un lampo tenda-su-avorio al decode. (a) accettarlo, (b) montare il JPEG e tornare alla tinta misurata, (c) altro.
19. **C04 / P05 / G04 — `fetchPriority="high"` sulla foto LCP**: in Next 16 `preload` mette solo il `<link>`; i docs consigliano `fetchPriority="high"`. Sul build locale l'LCP è già la foto a 44-128 ms: è un'ottimizzazione per la rete vera, una riga. Sì o no?
20. **R07 — sotto lg il pavimento di 100svh del blocco è stato tolto** da A46 (il blocco è alto quanto il contenuto): decisione di lavoro non scritta nel foglio del 22 set. Va bene?
21. **R08 — `.impeccable/design.json`** non è aggiornato da A38 (dice ancora «lettere sopra le immagini» sulle teste): entra nella chiusura documentale del commit 22.
22. **P04 — frangia azzurra dell'AVIF**: al bordo dell'alpha l'AVIF dell'ottimizzatore lascia più pixel azzurri del WebP (finestra 13 % del perimetro contro 7,7 %, /metodo 8,6 % contro 2,8 %; a 1× appena percettibile). Si accetta, o si alza la qualità delle sole foto col cielo (75) misurando i byte?
23. **P02 — «OPEN» sul tetto della facciata** a 1440×900 a schermo intero (contrasto 1,0:1 sotto la «O»): si risolve con A47 (foto alta C1 col cielo in cima); da riverificare lì con un test sui pixel sotto i glifi, non sulla griglia.
24. **P03 — tasche residue** (sopra): rifinire il criterio per colonna, o accettarle.

## Cosa resta (in ordine)

1. ~~Revisione avversaria di A46~~ **fatta** (commit aa4d2ce, esito nella sezione sopra); **A48 costruita** (sezione «A48 costruita» sotto). Restano A47, A49 (domanda 2), A50 (domanda 1), A51. Il testo originale della voce: **Revisione avversaria di A46** sul build (`git show c65e44f`): regole (DESIGN.md, 16 px, veli, curve, test non ammorbiditi), codice (geometria `cima`, `cieloH`, `margin-top` negativo, `overflow: clip`, CLS, sizes, reduced-motion e senza JS), browser (nove teste + finestra a 1440×900, 1024×640, 768×1024, 390×844, 360×640: pixel avorio sotto le scritte, nessun alone azzurro, nessun buco fra comandi e soggetto, contrasto ≥ 4,5:1, testata inchiostro, LCP ancora prioritaria). Un giro di correzione, commit.
2. **G2 / H01** (voce 1 del foglio): pista A, con la continuità GL ↔ DOM misurata a cavallo del passaggio.
3. Il foglio delle decisioni (voci 2-16), poi ciò che ne discende.
4. **Chiusura documentale del commit 22** (senza misure): registro §11 della spec del 10 settembre con le 30 righe A18-A26 / D16-D37 ancora «da costruire» (sono costruite dai commit 1-20), le 9 note, D33/D37 «superate da A38-A41», A27-A45 assenti (vivono in `docs/direttive-foto-entrata.md`, `docs/direttive-video-entrata.md`, spec del 20 settembre, messaggi di commit); PRODUCT.md e `.impeccable/design.json` col lessico vecchio («tre nastri», «TextLines», «una volta per sessione», «60svh»); commenti superati (`foto-alte.mjs:16-23`, `congedo.mjs:20-21`); `docs/da-chiedere-alla-cliente.md` (2.5, 2.6, 6.5, riga 17, 28-29); handoff del 13 settembre «Trovato durante il lavoro» chiuso voce per voce.
5. Commit 21 (se sì) e le misure di chiusura §9.3 (Passi 22.1, 22.3-22.9: axe con motion attivo, suite intera, LCP/CLS base contro ramo).
6. Fuori dal blocco: le nove foto generate non usate (corridoi C1/C2, scene H2/H3/A2, i quattro ritratti generati di Raffaela: per un volto generato serve il sì scritto della cliente); l'iframe di Google Maps su /contatti senza cancello del consenso (V11).

## A48 costruita (22 set., sera, sull'altro PC)

Le parole di Alberto stanno nel registro §11 (riga A48) e nella spec `2026-09-22-foto-sfondo-a47-a51.md`. Commit 8f417a0 «feat(teste): lo spazio sopra la foto (A48)».

- **Il meccanismo**: dentro lo strato della foto, dopo la scatola `.dt-testa_foto` (immagine `fill`, marcatori del segno, tinta di attesa), `.dt-testa_sopra` porta in flusso i tre punti e le sezioni che la pagina posa sulla foto (`PageHero` prop `sopra`). Da lg comincia alla **banda scura** della foto (`--dt-sopra-h` da `sopra[0]` di tinte.json, misurata da tinte.mjs per TERZI della colonna del testo 8-92 %: in ogni terzo al più il 35 % di pixel chiari, corse unite sotto il 3 %, la più lunga, minimo 8 % dell'altezza) in bianco nudo senza fondo, sotto il cancello `data-sopra="foto"`; dove non c'è banda (`sopra: null` → `data-sopra="carta"`: /recensioni, muro bianco a sinistra per tutta la foto) e sotto lg lo spazio sopra segue la foto, in inchiostro. La prima versione partiva al fondo del blocco: bianco sul cielo trasparente (la carta) e sui muri bianchi, invisibile (pellicole della prima corsa); la seconda misurava l'intera riga: bianco sul bordo assolato della piscina (66 % di chiari) e sul muro di /recensioni (96 %), presi dal nuovo test dei pixel. Da qui la misura per terzi.
- **Le rotte**: /vendi posa «Costi chiari» (era subito sotto l'hero); /acquista la testa della ricerca intelligente (`RicercaProvider` + `SearchHead` in `PropertySearch.tsx`, i filtri e i risultati restano in `#case`); /lavora-con-noi i soli tre punti; /recensioni i tre punti sulla carta dopo la foto (nessuna banda); /metodo e /open-domus non hanno punti né sezioni posate (voce 27).
- **Verificato sul build**: unit 2308/2309 (1 skip), tsc, lint; e2e a28 + search: a28 104 passati, 96 saltati, 0 rossi (ottava corsa, dopo sette giri di tolleranze per la coda a 1024); search 15/15 (quarta corsa, stesso codice). Nuovi test: tinte.test «la banda scura dello spazio sopra» (6), a28-fallback (scatola, spazio sopra, bianco solo da lg col cancello), a28.spec «sotto le scritte bianche la foto è scura» (quattro rotte a 1440 e 1024, ≤ 40 % di pixel chiari sotto ogni scritta, misurato sui pixel resi; /recensioni saltata: sulla carta) e «la ricerca sulla foto» (3).
- **Misure a 1440×900**: /vendi blocco 0-900, foto 430-2576, tre punti dalla banda (0,727 × 2147 + 430 ≈ 1991), «Costi chiari» dentro la foto sulla piscina, sezione dopo sulla carta; /acquista foto 37-2183, la ricerca sul prato (dalla banda 0,516: ≈ 1145), `#case` a 2183. A 390: i punti seguono la foto (585 px), in inchiostro.
- **Pellicole** in `2026-09-22-pellicole/a48-*.jpg` (1440×900 e 390×844, /vendi e /acquista, scatti ogni 0,85 schermi).

**Foglio delle decisioni, voci nuove** (25-28):

25. **A48 — più di uno schermo di sola villa fra i comandi e il bianco**: su /vendi la banda scura è la piscina oltre il bordo (0,727): fra «Come funziona» (830 px) e i tre punti (~1990) c'è più di uno schermo di facciata senza scritte, come la coda di «Architecture» su era; e a 1024 la sezione a due colonne sfora la foto di 15-44 px secondo la lingua (il cover scala la foto fino al 5 %, la e2e lo ammette fino a lì). Va bene così, o «Costi chiari» torna sulla carta e sulla foto restano i soli tre punti?
26. **A48 — il contrasto del bianco sulla piscina e sul prato** (~2,5:1, come il bianco sul mare di era): la deroga a WCAG 1.4.3 chiusa con A46 si riapre per queste scritte. Confermata?
27. **A48 — /metodo e /open-domus**: non hanno tre punti né una sezione da posare: quale sezione va sulla foto (la testa del primo capitolo?), o restano foto sola?
28. **A48 — /recensioni senza banda**: il muro bianco a sinistra corre per tutta la foto, il bianco non regge in nessuna riga: i tre punti restano sulla carta dopo la foto (`data-sopra="carta"`). Va bene, o si cambia foto (le riserve di A45: `villa-portico-glicine-alta`, `villa-ingresso-scala-alta`)?

## A52-A54 costruite (22 set., sera, sull'altro PC)

Le parole di Alberto stanno nel registro §11 (righe A52-A54). Stesso giro di A48, commit successivo a 8f417a0.

- **A52, la ricerca**: un blocco solo (`.dt-ricerca` in `PropertySearch.tsx`: occhiello, campo alla misura d4, stato, cinque tendine nella forma dei campi del modulo — zona per prima, e2e search.spec), sulla foto di /acquista; affinamenti (caratteristiche, venduti) come chip di testo sulla carta sopra i risultati; `RicercaProvider` porta anche comuni, scelte di budget e valuta.
- **A53, la chiusura**: `ChiusuraFoto.tsx` (client, montato da PageHeroTesta dopo lo spazio sopra): la scatola della foto si ritira nella cornice della cartolina mentre sale, come la banda del Congedo sotto la soglia; si arma solo con una coda libera ≥ ¼ viewport.
- **A54, riempire con l'ombra**: la banda scura per terzi (costruita nel pomeriggio: `sopra` in tinte.json, `misuraSopra`, ancoraggio al fondo) è TOLTA: lo spazio sopra parte dal fondo del blocco, bianco con l'ombra di ink-media.ts (terza occorrenza ammessa da ink-media.test), corpi alla misura del lead; `data-sopra="foto"` su tutte le teste con la villa. Posano: /vendi punti + Costi chiari + tre leve; /acquista punti + ricerca; /metodo le tre leve; /open-domus il claim. Da riempire ancora: /servizi (Services ha foto), /chi-siamo (prima sezione con foto), /domande-frequenti (indice sticky), la home (A49).
- **Foglio delle decisioni**: le voci 25-28 sono superate da A54 (niente carta, ombra ammessa, /metodo e /open-domus riempiti, /recensioni sulla foto con l'ombra). Nuove: **29** — con l'ombra le sezioni posano anche sui muri bianchi della villa (/vendi le tre leve sulla facciata): va bene o su quelle righe si preferisce l'inchiostro? **30** — /servizi, /chi-siamo, /domande-frequenti: quale sezione va sulla foto (le prime hanno foto o un indice sticky)?

## A55 costruita (22 set., pomeriggio, sull'altro PC)

Le parole di Alberto stanno nel registro §11 (riga A55). Commit af757aa.

- **La foto**: l'hero della home è la foto vera della piscina (`hero-raffaela-piscina.jpg` 1920×1280 e la striscia 9:16 `-m.jpg` 720×1280 da x 660, da `villa-pool.jpg` con `scripts/media/hero-piscina.mjs`, senza metadati); la riga «Acquista» di Paths mostra la scena generata di A44/A45 (`hero-raffaela-villa.jpg`, `pos: "50% 100%"`, alt in cinque lingue); `--dt-hero-ar` 720/1280 e 1920/1280; `hero-raffaela-villa-m.jpg` è uscito dal repo. hero-dive.test legge i file da media.ts e ne confronta le misure con `baseSize`/`baseMSize`.
- **Il lockup**: Raffaela sta al centro della foto, dove stava il lockup; «Domus Tua» è sceso in basso (`justify-end`), a destra da lg (`lg:items-end lg:text-right`), sull'acqua, con la sua mano tesa che lo presenta. Da lg le lettere scendono da 13vw a 10,5vw (`--text-hero-lg` in globals.css, solo l'hero: il preloader resta a 13vw), perché sulla pellicola del build a 13vw la «D» le copriva le gambe a 1440 (lockup da 700, lei fino a 770) e a 1024 (498 contro 560). Scelta di qualità mia (A27: mai coprirla), da mostrare ad Alberto: **voce 31** del foglio (posizione e misura del lockup).
- **La maschera del preloader**: Alberto, a lavoro in corso: «e se usassimo la maschera del preloader vecchia di Raffaela? La preferisco, e poi all'entrata ci sarà la foto nuova, la maschera se ne va via con l'entrata ad arco sulla hero» → resta `raffaela-sagoma-villa*.webp`; le due figure non coincidono più, il patto della porta è la scatola (intro-clocks.test, DESIGN.md «Il patto della porta»). Un ritaglio dalla piscina (Higgsfield `remove_background`, 1 credito: ne restano 9) era stato fatto, ripulito e scartato.
- **Pellicole** (build finale, dopo il preloader, banner dei cookie chiuso): `2026-09-22-pellicole/a55-hero-1440.jpg`, `a55-hero-1024.jpg`, `a55-hero-390.jpg` (il lockup in basso a destra sull'acqua, Raffaela intera al centro), `a55-paths-1440.jpg` e `a55-paths-390.jpg` (la scena generata nella riga «Acquista»).
- **Verifica**: npm test 2304 (2303 verdi, 1 skip), tsc e lint puliti; e2e hero-dive + preloader-corta 43/43 (13 skip) in tre corse, l'ultima sul codice finale; home 107/109: i due rossi sono Voci (rapporto 10 al ricaricamento, mobile-390) e la cartolina del Congedo sul telefono (inset 4/10 non raggiunto), entrambi fuori da hero e Paths e già nella lista dei rossi preesistenti qui sotto.

## A47 costruita (e3d7627), e A54 su /chi-siamo (22 set., pomeriggio, sull'altro PC)

Alberto, dopo A55: «continua, con tutte le direttive su spazi e foto che ti ho fornito». Le parole di A47 stanno nel registro §11.

- **A47, la finestra intera**: `scripts/media/cielo.mjs` maschera la facciata che sale (`villa-facciata-sale-alta`, 9:16, cielo 34,9 %, cima 0,246) al posto della 3:2 col glicine (resta su disco senza WebP); `scripts/media/finestra.mjs` scrive `app/lib/motion/finestra.json` (file, sorgente, cielo, bande del segno: le travi delle pergole a 0,289-0,347, 0,408-0,481, 0,61-0,672). OpenDomus.tsx: la cornice tiene titolo, foto (`.dt-od_window` col rapporto della sorgente, `object-position 50% 0%`, `sizes 100vw`) e il capitolo (`.dt-od_content`, dentro la cornice); da lg la cornice ha il rapporto della foto, la scatola la riempie e il capitolo posa dal 55 % (`FINESTRA.sopra`, `--dt-od-sopra`) in bianco con l'ombra (quarta occorrenza in ink-media.test); il marcatore `foto-chiara` arriva al fondo dell'area, i marcatori `foto` sulle travi. La pista di 200svh e le tende restano; `finestraFocusY` prende `contentTop`. Test: finestra.test (numeri, json/CSS, markup, regole), tinte.test (facciata 9:16), ink-media.test; e2e home (marcatori, foto che continua, capitolo sulla metà bassa, cielo campionato sotto la testata sul telefono, trave della pergola), corridors (capitolo in vista dopo la pista), segno (facciata dalla geometria: sopra l'img sta il capitolo), motion (9:16 con reduced motion).
- **A54 su /chi-siamo**: i valori (Highlights) posano sulla foto della testa (l'attico, senza cielo) come le tre leve su /metodo; la storia resta sulla carta con la sua foto (un modulo foto dentro la foto non si posa). Pellicola `2026-09-22-pellicole/a54-chi-siamo-1440.jpg`: il titolo regge sulle travi, le tre voci sui muri chiari dell'attico reggono poco — la voce 29 vale anche qui (e per il capitolo sulla facciata della finestra, A47).
- **/servizi e /domande-frequenti restano coi soli tre punti** (voce 30 del foglio, precisata): il capitolo Servizi intero sfora la foto oltre il 15 % (a28.spec: la coda scalerebbe la foto e il cielo) e spezzarlo violerebbe l'atomicità delle sezioni (spec A48); le FAQ sono 3-5 volte la foto. Serve una sezione corta da posare, o si accetta la coda.
- **Verifica**: unit 2305 (1 skip), tsc, lint; e2e home + corridors + segno + motion 195 verdi in 9,2 min (cinque rossi: Voci preesistente e quattro difetti dei test nuovi, corretti: il rettangolo del capitolo porta il padding, la testata copre il cielo sul telefono, sopra l'img sta il capitolo), poi home + corridors + segno + a28 mirati 24/24. Pellicole: `2026-09-22-pellicole/a47-finestra-{1440,1024,390}-*.jpg`.

## A51 e A56 (8fe368e e 423c188, 22 set., pomeriggio, sull'altro PC)

Alberto, guardando il dev server: «lo sfondo è ancora bianco, quando avevamo detto di metterlo sul rosa tramonto» (A51) e «le scritte bianche sopra le immagini, mettile di colore grigio, come quello della hero della scritta "domus"» (A56). Parole nel registro §11.

- **A51, la carta rosa tramonto**: `--color-cream` #f9f5ef → #f6d9d0 (rosa tramonto opaco, chiaro: inchiostro 8,5:1, rosso 4,6:1), e la famiglia con lo stesso passo: `--color-cream-deep` #f2cfc5, `--color-paper` #fdf1ec, `--color-line` #e9c9c0; `--color-stone` scende a #625d56 (5:1) e i testi piccoli in rosso (occhielli, etichette e link a 16 px, corpi, numeri non display, il bottone a testo rosso: regola in globals.css accanto a `.dt-btn--cta`) passano al rosso cupo #a30707 (6,1:1), perché il rosso vivo sul rosa fa 4,2:1; `--background`, `themeColor` (layout.tsx), l'avorio di tinte.mjs, cielo.mjs e foto-alte.mjs, tinte.json rigenerato (29 righe: le tinte alte degli interni si ricalcolano contro il nuovo avorio), il sidecar `.impeccable/design.json`, tinte.test e le costanti degli e2e (a11y, a28, home, segno, preloader-corta). I WebP col cielo non cambiano (la decontaminazione usa il colore del cielo, non la carta). Da mostrare: la tinta, e se la vuole più rosa.
- **A56, il grigio sopra le foto**: le sezioni sopra le foto delle teste e il capitolo sulla facciata della finestra sono in `--color-graphite` (#46423d, il «Domus» del lockup), senza ombra: ink-media.test torna alle due occorrenze di D80; a28-fallback, finestra.test e gli e2e (a28: punti e campo della ricerca, home: occhiello della finestra) misurano il grigio e `text-shadow: none`. Segnaposto e righe dei campi in `color-mix` del grigio (72 % e 45 %). La voce 29 del foglio (bianco sui muri chiari) è superata.
- **Il render «tagliato» dello screenshot di Alberto** (la finestra col capitolo sull'avorio e la facciata che finisce): era il dev server Turbopack, non il build (le pellicole di A47 sul build mostrano la facciata intera); il dev è stato riavviato e rimisurato con Playwright sulla 3000: stessa geometria del build (capitolo a 1.419 px dalla cima dello stage a 1440).
- **Pellicole** (build finale): `2026-09-22-pellicole/a51-home-hero-1440.jpg` (la carta rosa sotto la testata e la piscina), `a56-vendi-1440.jpg` e `a56-acquista-1440.jpg` (le strisce delle due teste col grigio sulle foto: Costi chiari, i punti, la ricerca), `a56-finestra-1440-capitolo.jpg` (il capitolo grigio sulla facciata).
- **Verifica**: tsc e lint puliti; npm test 2305 (2304 verdi, 1 skip); e2e a28 + search + a11y 153 verdi (rossi: a11y su /valutazione, corretto con l'avorio profondo #f2cfc5, e la tolleranza nota della chiusura sul telefono), poi a11y + segno + preloader-corta 88/88 e home (finestra) + a28 (chiusura) 7/7 sul codice finale.
- **A53 sulla finestra** (22 set., pomeriggio: «qua, la foto, come nelle altre pagine con le foto a schermo intero no bg, deve rimpicciolirsi alla fine»): ChiusuraFoto riconosce anche la finestra (`data-od`: cornice, scatola `.dt-od_window`, capitolo) ed è montata nella cornice dopo il capitolo; a fine coda la facciata si ritira nella cornice 8/22 come le teste. Si arma con una coda libera ≥ ¼ viewport: a 1440 sì (coda ~260 px), a 1024 no (il capitolo arriva al travertino).
- **Foglio delle decisioni, voce 32**: col grigio (A56) le scritte sopra le foto leggono bene sui muri chiari, sul travertino e sul cielo-carta, e perdono sulle bande scure — le travi delle pergole e il glicine della finestra, l'acqua e le siepi delle teste (/vendi «Costi chiari» sulla piscina, i punti di /domande-frequenti sulla piscina di notte). È il rovescio della voce 29. Due leve, da scegliere con Alberto: spostare le sezioni sulle bande chiare (la quota del 55 % della finestra, `FINESTRA.sopra`), o un grigio per banda letto da tinte.json (le bande `segno` dicono già dove la foto è scura).

## A57-A62 costruite (8e5c633, 22 set., sera)

Alberto ha mandato la lista «Cosa manca, per la nuova sessione» coi punti 5-10 e due screenshot; le parole esatte stanno nel registro §11 (A57-A62).

- **A57 + A58, il nastro della finestra**: `OpenDomus.tsx` (ramo `finestra`) monta `HorizonScroller` con la prop nuova `lead` (la salita: la cornice della facciata sale dentro lo schermo agganciato di `cima × altezza − 0,1 × vh`, finestra.ts `leadDistance`, poi il track scorre di lato) e la firma del capitolo per prop (`ease`, `scrub`: dtInOut, 0,15 — D18). Tre pannelli: la facciata intera col titolo sul cielo-carta; claim + video di Teresa a sipario; liste + CTA + Raffaela sulla soglia (`raffaela-porta-alta.jpg`, alta 80svh) a sipario. Morti: tende, stage, pista, marcatori da 1 px, spazio sopra sulla foto, chiusura in cartolina, `useCorridor` per la finestra (`CorridorId` = hero, cartolina). La rete di fuoco del nastro (Tab su un pannello fuori scena → la pagina va alla quota giusta invertendo l'ease per bisezione) vale per tutti e due i nastri. CSS: il blocco «LA FINESTRA DI OPEN DOMUS» riscritto; `finestra.test.ts` riscritto; `data-bg.test` senza `.dt-od_mark`; e2e home/corridors/motion/segno riscritti.
- **A59, il foglio delle foto di D.O.C.**: `DomusDocProtocol.tsx`: lista in colonna + cornice sticky (`[data-doc-cornice]`, da lg, `min(42vw, 640px, 80svh × 2/3)`, top 10svh) con cinque foto assolute; il pilastro che attraversa il 62 % è l'attivo (`data-doc-attivo`, `data-doc-vivo` sul foglio): la sua foto entra a tendina (dal basso scendendo, dall'alto risalendo) con scala 1,08→1 in 1,1 s dtOut (tratto «sfoglio» in chapters.ts `doc`; il file è nei DICHIARATI di chapters.test) e la cornice prende l'altezza del sorgente. Trattino attivo in transform (origine a destra), pilastri inattivi in pietra solo col foglio vivo. Sigillo: anello di testo `textPath` a 16 px, 24 s a giro in CSS. La spina della Hairline ora separa lista e cornice (`hidden lg:block`; doc-services.test aggiornato). **Deroga**: è una quarta cosa sticky oltre i tre nastri (DESIGN.md «Don't aggiungere sezioni sticky»): da mostrare ad Alberto.
- **A60, il video di Roberta**: `StoryVideo.tsx` (nuovo) in `HorizonStory` al posto di `LazyYouTubeEmbed`; `useAmbientVideo` con `audio: true`, `minWidth` aperto e `ar`/`sdWidth` (ambient.ts: `renderedWidth` prende il rapporto, `pickAmbientSource` la larghezza sd); `data-user-muted` sul `<video>` tiene tolto l'audio tolto col comando (`zittito()` nel hook; ambient-video.test aggiornato). File: `public/media/open-domus-roberta-{1080,720}.{mp4,webm}` + `-poster.jpg`; sorgente in `Downloads/🎬 STORIE DI SUCCESSO … _1080p.mp4` (1080×1920, 86 s). Icone `Pause`, `SoundOn`, `SoundOff` in Icons.tsx.
- **A61, le foto degli atti**: Method.tsx `ACT_IMAGES` con `w`/`h` e la scatola al rapporto del sorgente (`ClipMedia` prende `style`); alt nelle cinque lingue.
- **A62, l'inventario dei media**: `docs/media/inventario-2026-09-22.{md,html}` da `scripts/.probe/inventario.mjs`.
- **Verifica**: npm test 2296 verdi; tsc e lint puliti; sul dev di Alberto (3000, agganciato dal Browser pane): nastro acceso con tre pannelli e salita 375 px a 1024×768, foglio D.O.C. che sfoglia, video che suona in vista (720.webm) e sul telefono; e2e home + corridors + motion + segno: prima corsa con i rossi attesi (test vecchi della finestra, `.dt-horizon` ambiguo, il colore del filo di A51 in motion.spec, la soglia alta quanto la riga) tutti corretti; seconda e terza corsa (`scripts/.probe/a57/e2e-2.log`, `e2e-3.log`): 36 + 5 verdi, restano i rossi preesistenti di Voci.
- **Trappole nuove**: il gate ECC nega il PRIMO Edit/Write di ogni file, sempre: un ritocco minimo prima sblocca la scrittura vera; `wheelTo` degli e2e può fermarsi 81 px oltre la quota; il Browser pane cambia altezza fra un giro e l'altro (le quote misurate prima non valgono più); il dev server di un'altra chat sulla 3000 si aggancia con `preview_start { url }` e riflette il codice, ma la CSS di Turbopack può essere stantia.

## A63-A68 costruite (8a509f8, 22 set., sera, dopo 8e5c633)

Alberto ha guardato il dev e ha mandato cinque ritocchi (registro §11, A63-A67).

- **A63**: `StoryVideo` in HorizonStory a `lg:!w-[36vw] lg:!max-w-[560px]`.
- **A64**: pilastri di D.O.C. a `py-12`, riga in `text-lead` nella home.
- **A65**: `lead.selector` = `.dt-od_window` (non più la cornice): il titolo resta fermo, la scatola sale finché `cima × h − (titolo.offsetTop + 0,5 × titolo.offsetHeight)`; `FINESTRA.copri`.
- **A66**: claim in d2 (`SplitTitle`) + intro `Lead` nel secondo pannello; titolo a gradini (`data-horizon-stair`, tre righe, `copy.stairs` nelle cinque lingue) + liste in lead + rilancio nella coda (`.dt-od_coda_testo`, assoluto sul pannello nel nastro, in flusso sotto la soglia).
- **A67**: `tail` di HorizonScroller (`selector`, `also`, `from`, `distance`): con salita o coda le quote sono esplicite (`esplicito`): altezza = salita + corsa + coda + schermo, track da `top+=salita top` a `top+=salita+corsa top`, coda scrubbata dopo (foto da `from` a `from − coda`, scritte da 0 a `−coda`). La foto è `villa-piscina-lunga-alta-cielo.webp` (scelta di Alberto), misurata in `app/lib/motion/coda.json` da `scripts/media/finestra.mjs <nome> <uscita>` (ora parametrico). `.dt-od_porta` e `PORTA` sono morti.
- **Test**: finestra.test riscritto (copri, codaFrom/codaDistance, coda.json, HorizonScroller esplicito), split-title-migrazione OpenDomus 2 (il titolo a gradini), motion.spec (stili nulli con reduced motion), home.spec finestra desktop riscritto (salita sulla scatola, tetto a metà titolo, arrivo e fine della coda). Corse: `e2e-4.log` (1 rosso: il checkpoint della coda a 1024 con lo sforo di 81 px di wheelTo, tolleranza allargata), `e2e-5.log` (finale).
- **A68**: la chiusura in cartolina della coda (OpenDomus.tsx, `useGSAP` con lo span `data-chiusura-coda`: trigger la sezione, «bottom bottom» → «bottom 10%», scrub 0,9, dtCartolina, `CORNICE_LG` esportata da ChiusuraFoto, clip sulla scatola `.dt-od_coda`).
- **Trovato misurando**: la sezione di Voci si accorcia di ~80 px dopo il primo passaggio (1506 → 1426 a 1440×900) e ScrollTrigger non rifà il refresh: tutto ciò che sta sotto (Metodo, finestra, D.O.C., …) ha le quote spostate di 80 px finché non arriva un resize. Il test della finestra rilegge la quota dal DOM prima della coda; la causa (probabilmente un embed o una copertina che cambia altezza dopo il carico) è da cercare in Voci.tsx. Nello stesso giro `HorizonScroller` misura il track con `offsetWidth` invece di `scrollWidth` (che contava lo sbordo dei gradini in parallasse, ±80 px a ogni refresh).
- **Da mostrare**: nella coda le scritte posano sulla carta del cielo e scendono con la foto; a fine coda le liste stanno sui cipressi/sul prato (contrasto del grigio da guardare); il rapporto salita/corsa/coda è 1:1.

## Risposte di Alberto della sera (A69-A71, commit 0676de0) e il piano di A49

- **A69**: `villa-piscina-facciata.jpg` (Salerno, 3:2) al posto del salotto doppio nell'atto «Poi, le immagini»; in D.O.C. «Preparazione» prende `villa-terrazza-colline-alta.jpg` (generata). Il salotto doppio resta nel repo, non montato. La licenza delle foto di Salerno (voce 10 del foglio) resta da chiudere: ora una è in pagina.
- **A70**: le scritte sopra le foto delle teste tornano bianche, più grandi (titoli ×1,2, corpo d4 a 500), senza ombra; Alberto le testa e, se non si leggono, si abbassa la luminosità della foto.
- **A71, il piano di A49 (Alberto: «sì, fallo, anche il voto e i due link; prova un upscale se ho crediti; falla no-bg»)**: (1) Higgsfield: outpaint della piscina (`f6c29f75-…`, 1920×1280) a 2:3 1920×2880 (job `453a6ceb-…`, 2 crediti), poi upscale 4k (2 crediti; 9 disponibili); (2) scaricare in `public/media/hero-raffaela-piscina-alta.jpg` e mascherare il cielo con `scripts/media/cielo.mjs` (voce nuova in FOTO) → `-cielo.webp`, misure con `finestra.mjs <nome> app/lib/motion/hero.json`; (3) HeroCinematic: la banda diventa la foto alta in flusso come le teste (niente corridoio `hero` di useCorridor, niente tuffo/zoom), con H1, CTA, voto e i due link posati sulla foto (lo schema `sopra` di PageHero, A70 in bianco) e la chiusura in cartolina (ChiusuraFoto); il lockup e la firma restano; (4) preloader: il patto della porta è la scatola della banda (`--dt-band-h`, `--dt-hero-ar`): con la foto alta la sagoma resta nel primo schermo; (5) test da rivedere: intro-clocks, corridor-contract (il corridoio hero), chapters (firma `hero`), data-bg (marcatore dell'hero), e2e hero-dive, preloader-corta, home (hero), a28, segno, corridors (EXPECTED_HOME senza `hero`), motion, mobile-motion.

## A49 costruita (52fad5e, 22 set., notte)

L'hero alto, secondo il piano di A71 («sì, fallo, anche il voto e i due link … e falla no-bg»). Le decisioni sono numerate D-A49-1..6 e stanno nei commenti di `hero.ts`, `HeroCinematic.tsx` e del blocco «L'HERO ALTO DELLA HOME» di globals.css.

- **Media**: `scripts/media/hero-piscina.mjs` riscritto: ritaglia dal WebP alto col cielo la striscia 9:16 del telefono (`hero-raffaela-piscina-alta-m-cielo.webp`, 2144×3812, centrata: Raffaela intera, escono 208 px per lato) e scrive `app/lib/motion/hero.json` (foto e striscia: sorgente, cielo cima/linea, bande del segno). `media.ts` monta i due WebP (base 2560×3812, baseM 2144×3812, poster il JPEG alto); i due JPEG di A55 in public/media restano non montati.
- **Numeri** (`app/lib/motion/hero.ts`, misurati con `scripts/.probe/a49/acqua.mjs`): cielo 0-0,192; villa 0,19-0,60; Raffaela 0,48-0,615 (x 0,46-0,56); acqua 0,62-0,92, CHIARA (L 0,70 a sinistra); banda scura = il portico a destra di lei, 0,42-0,60 (L 0,28-0,39). Quote: `testo` 0,42, `acqua` 0,68, `coda` 0,85, `cotto` 0,07; `salitaRiposo = min(cima·H, max(0, testo·H − banda))`.
- **Componente**: `HeroCinematic.tsx` è la testa di era senza blocco (`.dt-testa dt-hero`, `data-testa`, `data-sopra="foto"`, riquadro → strato → scatola della foto con `<picture>` e le bande → spazio sopra con la posa [marca, blocco] → `ChiusuraFoto`). Niente useCorridor, zoom, lift, marcatore 1 px; `CorridorId` = "cartolina"; Posizionamento è `#posizionamento` senza `data-hero-cover`. Il rito delle lettere suona per gruppo (lockup, H1, firma) all'handoff se in scena, altrimenti alla prima entrata (IntersectionObserver). `ChiusuraFoto` prende `ease`/`scrub` per prop (default delle teste); l'hero passa `chapters.hero.signature.ease` e `scrubOf("hero")`.
- **CSS**: token `--dt-hero-ar`/`--dt-hero-hw`/`--dt-hero-cima` per fascia (2144/3812 → 2560/3812 da 768); blocco `.dt-hero`: margin-top 0 (la sezione sta SOTTO la testata: il patto della porta è la banda), salita a riposo con `clamp()` in percentuali della larghezza, spazio sopra da `acqua` (sotto lg) o `testo` (da lg), posa con aspect-ratio (coda − testo)·hw da lg, marca con aspect-ratio (1 − acqua)·hw e margini negativi della riga sotto lg, H1 bianco ×1,2 da lg, «Domus» grafite dentro lo spazio bianco. Il corridoio dell'hero e il foglio sono morti.
- **Registro**: `chapters.hero` = dtCartolina, scrub 0,9, innesco sullo strato di #top; secondario dtOut (il rito). D18 fra hero e cartolina non vale (`MOTIVO_COMUNE` in chapters.test: D-A49-6). `scrubOf("hero")` = 0,9.
- **Test**: unit 2291/2292 (1 skip): hero-alto.test (nuovo, al posto di hero-dive), intro-clocks (il patto: la salita legge `--dt-band-h`, la foto dalla cima), corridor-contract (solo cartolina), chapters (MOTIVO_COMUNE, scrubOf), data-bg (bande dell'hero, elenco 1 px vuoto), corridor-math, motion-tokens, congedo-cartolina, media-file (commento). tsc e lint puliti. E2e riscritti: hero-alto.spec (geometria a 1440/1024, chiusura 8/22, lettere alla prima entrata, telefono, senza JS, tedesco), corridors (EXPECTED_HOME senza hero), home (Posizionamento senza foglio), motion (reduced motion), lama, segno.
- **Verificato sul dev (3000) a 1440×900**: salita 82 px, blocco a y 900 (la piega), x 849-1310, H1 bianco 48 px; marca 1471-1812 (0,69-0,85), coda libera 318 px; lettere a 0,02 a riposo e a 1 dopo l'entrata; clip `inset(8% 22%)` a fine foto. A 1024×768: salita 0, il sovratitolo fa capolino di 60 px al fondo del primo schermo (la foto è più corta del blocco). A 390×844: striscia 9:16 (693 px), lockup centrato sull'acqua, blocco dopo la foto in inchiostro.
- **Trappole nuove**: next.config `images.qualities` è [60, 75, 78]: un `quality` fuori lista viene servito a 75 in dev (in build può fallire) — l'hero sta a 75; l'aspect-ratio di un figlio di `.dt-row` si misura sulla larghezza al netto del padding (la marca esce coi margini negativi); Chrome risolve `aspect-ratio: 1 / calc(…)` con var() (posaAR "1 / 0.640313").
- **Aperto / da mostrare**: la piega a riposo (cielo-carta nel primo schermo: 322 px di carta sotto la testata a 1440×900); il lockup non più sul bordo ma a 0,68-0,85 (per lasciare la coda alla cartolina); il blocco a destra allineato a sinistra; la leggibilità del bianco sul portico (A70 la testa Alberto).

## A73 costruita (99595cc, 22 set., notte)

Alberto, vedendo l'hero alto: «la foto della hero va modificata, usa Higgsfield per togliere il bg (ha qualche problema), e mettere la foto di Raffaela (attualmente è irriconoscibile perché modificata da Higgsfield). Usa quella del preloader … non devono cambiare né rapporti, grandezza, né qualità».

- **Cosa**: `scripts/media/hero-raffaela.mjs` (nuovo) fa un fotoritocco sulla foto alta: (1) il PLATE senza la donna chiesto a Higgsfield (`gpt_image_2_5`, 2k = 1360×2048, quality high, 3 crediti; job `ef80fd4b`; il PNG sta in `Downloads/higgsfield` col JPEG pre-ritocco `hero-raffaela-piscina-alta-upscale.jpg`, fuori repo come i PNG di foto-alte), allineato all'originale (SAD sulla balaustra: dx 1, dy 0) e usato SOLO nelle schegge della figura alterata (braccio destro, fianco, capelli: la maschera è la differenza originale/plate su copie sfumate, soglia 30, dentro il rettangolo della figura, meno la figura nuova e le gambe; 17.387 px); (2) le gambe sotto il ginocchio restano quelle della foto; (3) `raffaela-sagoma.png` (il ritaglio vero in pizzo del preloader) riscalato a 274×352 (testa→ginocchio della figura vecchia) e posato a (1220, 1833). JPEG 2560×3812 q92 4:4:4 (1,23 MiB).
- **Sfondo**: il «problema» era la tasca di cielo pallido fra le fronde dell'albero sul bordo destro (203,229,248: L 0,88 ma S 0,76 in HSL, scartata dal punteggio di giorno): cielo.mjs ha ora `pallido` (per foto: L ≥ 0,78, tinta 190-235°, B − R ≥ 28 → cielo) più `raggio 700` e `libere 0,42` sull'hero. Nessun credito speso per lo sfondo (remove_background di Higgsfield non serviva). Crediti restanti: 2.
- **Numeri**: hero.json rigenerato (cima 0,194 → `--dt-hero-cima` 0.194 da 768; telefono 0,193; segno 0,269-0,694), striscia 9:16 rigenerata (467 KB).
- **Verifica**: unit, tsc, e2e hero-alto sul build (vedi il commit); pellicole in `scripts/.probe/a49/a73/` (composita5-zoom.png, cielo-destra-3.png, cielo-intera-2.png).
- **Trappole**: sharp riporta un raw a 1 canale a 3 canali dopo blur/threshold (`.toColourspace("b-w")` prima di `.raw()`); il rendering di gpt-image è traslato/morbido: mai incollare un rettangolo intero, solo le schegge; `models_explore get gpt_image_2_5` per i ruoli (`image_references`), `get_cost: true` per il preventivo (1k 1, 2k 3, 4k 4,5 crediti).
- **Da mostrare**: la figura in pizzo sui pantaloni bianchi (la gonna finisce al ginocchio, sotto i pantaloni della foto); l'ombra vecchia sul pavimento resta; la figura in pizzo non ha ombra.

## Da sapere prima di toccare il codice

- **Verifiche solo su `next build` + `next start`**: Turbopack dev serve la CSS con un'edizione di ritardo. La suite e2e (`npm run test:e2e`, `playwright.site.config.ts`) fa il build da sé sulla 3177 con `NEXT_PUBLIC_ENABLE_ASSISTANT=true`, `NEXT_PUBLIC_USE_REALSMART=false`, `REALSMART_ALLOW_MOCK=true`: lanciarla **per file**, mai intera (sotto carico un worker Chromium resta appeso e i test falliscono per tempo, non per difetti: memoria «Rivista bianca»). Non lanciare due build in parallelo.
- **Test rossi preesistenti, non legati ai blocchi 23-24**: `mobile-motion` «sipario a sessione fredda … PRIMO tocco» su tablet-768 (tempo); a 1440 sotto 4 worker «CLS 0» di a28 (font dopo il primo paint, 0,02-0,05); `hero-dive:362` e `:283`, HomeSearchGateway, Voci, D.O.C. ×2, cartolina al 60 % (rossi anche sull'albero di `1a0f8bf` a carico basso, secondo il bisect dell'implementatore di A46: da riverificare a macchina scarica prima di crederci).
- **Non sono su questo PC né sul repo**: il master «Tradate Via Cima rossa.mov», gli originali di Salerno, i PNG di Higgsfield, ffmpeg (tutto sul portatile); `progress.md` e `qualita/…` delle sessioni del 18-20 settembre; `reverse-engineering/era-residence/` (gitignorato). Crediti Higgsfield: 9 (uno speso il 22 set. per il ritaglio dalla piscina, poi scartato).
- **Regole del progetto** (AGENTS.md → `node_modules/next/dist/docs/` prima di scrivere codice Next; DESIGN.md e PRODUCT.md battono le skill): test prima del codice; commenti nuovi che dicono chi ha chiesto cosa (A/C/D) e com'è fatto; staging file per file, mai `git add -A`; messaggi di commit in italiano chiusi da `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; divieti (curve, card, fiori, nero, veli, blur, testo < 16 px, pin di GSAP, transform su antenati di sticky/fixed, scroll-hijack sul telefono, testo nascosto in CSS); /case/[slug] non cambia movimento; reduced-motion e senza JS = pagina completa e ferma; `--dt-head-h` e `--dt-band-h` restano condivisi.
- **Workflow multi-agente**: se il run muore per rete o per il limite settimanale dell'account (reset alle 22:00 Europe/Rome), l'albero di lavoro conserva ciò che l'implementatore aveva scritto: si riparte da lì, non da zero. I subagenti vedono come «richiesta dell'utente» l'ultimo messaggio: scrivere per esteso nel prompt la richiesta originale, altrimenti rifiutano di costruire.
