# Prompt per la sessione nuova: A49 (hero alto) e A72 (Costi chiari come nastro)

Incolla da qui in giù.

---

Lavoro sul sito Domus Tua (`C:\Users\alber\domus-tua-site`, branch `claude/rivista-bianca`, PR #79 aperta). Sono Alberto. Lavori a mano, senza sottoagenti né workflow anche se la sessione dice «ultracode on»; le scelte di qualità visiva le prendi tu col metro Awwwards e le dichiari come decisioni numerate; a me chiedi solo ciò che non puoi sapere. Le foto non si tagliano mai, soprattutto dove c'è Raffaela.

Prima di toccare codice leggi: `AGENTS.md`, `DESIGN.md` (sezione Shapes, «La finestra di Open Domus», «I set piece pilotati dallo scroll»), `PRODUCT.md`, il registro §11 di `docs/superpowers/specs/2026-09-10-redesign-rivista-bianca-design.md` (righe A47-A71), il handoff `docs/superpowers/handoff/2026-09-22-blocco-23-24.md` (sezioni «A57-A62», «A63-A68», «Risposte di Alberto della sera (A69-A71) e il piano di A49», «Da sapere prima di toccare il codice»), e la memoria di progetto `domus-direttive-22-set.md`. Skill da invocare: `impeccable` (direzione), `gsap-scrolltrigger` (tecnica), `verification-before-completion` alla fine; per ogni fase di' in una riga quali usi.

Stato al commit `ed1d0fb` (22 set. 2026, sera): la finestra di Open Domus è un nastro (`HorizonScroller` con le prop `lead` = salita, `tail` = coda, `ease`/`scrub` dal registro), tre pannelli (facciata che sale dietro il titolo fermo; claim d2 + video di Teresa; coda con la piscina lunga no-bg, titolo a gradini e liste sopra, che poi scende 1:1 e si chiude in cartolina). I numeri stanno in `app/lib/motion/finestra.ts`, `finestra.json`, `coda.json`; il CSS nel blocco «LA FINESTRA DI OPEN DOMUS (A57/A58)» di `app/globals.css`; i test in `app/lib/__tests__/finestra.test.ts` e nel blocco «la finestra di Open Domus» di `e2e/home.spec.ts`. Copia questa meccanica, non reinventarla.

## Blocco 1 — A49: l'hero alto (le mie parole: «non c'è né l'immagine alta che fa da sfondo pagina a schermo intero, né l'effetto dello scroll dentro l'immagine»; «sì, fallo, anche il voto e i due link; falla no-bg»)

Materiale pronto: `public/images/reali/hero-raffaela-piscina-alta.jpg` (2560×3812, la piscina di Raffaela estesa a 2:3 con Higgsfield e upscalata) e `-cielo.webp` (cielo trasparente da `scripts/media/cielo.mjs`, voce `hero-raffaela-piscina-alta`); misure in `app/lib/motion/hero.json` (cima 0,192, linea 0,42, banda del segno 0,269-0,695).

Da costruire in `app/components/HeroCinematic.tsx` (637 righe, oggi: corridoio `hero` di `useCorridor` con stick bottom, tuffo/zoom `[data-hero-zoom]`, lift `[data-hero-lift]`/`[data-hero-block-lift]`, banda `[data-hero-media]` alta `--dt-band-h`, `<picture>` con `heroCinematic.base/baseM` da `app/lib/media.ts`, lockup «Domus Tua» e firma coi riti del preloader `data-hero-char/tchar/schar`, marcatore `data-bg="foto"` largo 1 px):
- la foto alta in FLUSSO come le teste (`PageHeroTesta`: riquadro = la carta, foto col cielo trasparente, `sizes` 100vw, nessun cover che ritagli), senza corridoio, senza tuffo, senza zoom, senza lift; le scritte salgono con la foto come se fossero nella foto;
- il lockup «Domus Tua» e la firma restano dove sono (in basso a destra sull'acqua, 10,5vw da lg: A55);
- H1, CTA «Richiedi la valutazione», il voto (`ratingLabel`) e i due link «Vendi casa» / «Cerco casa» posano SULLA foto, nella banda scura (lo schema `sopra` delle teste, `.dt-testa_sopra`), in BIANCO senza ombra e grandi (A70: `.dt-testa[data-sopra="foto"]` in globals.css);
- all'uscita la foto si chiude in cartolina (`ChiusuraFoto`, cornice 8/22, dtCartolina);
- il patto della porta col preloader resta sulla scatola della banda (`--dt-head-h`, `--dt-band-h`, `--dt-hero-ar`: `intro-clocks.test.ts` lo presidia): l'arco si apre sul primo schermo della foto, la sagoma in pizzo `raffaela-sagoma-villa*.webp` non cambia;
- telefono: decidi tu fra la 2:3 a tutta larghezza e una striscia 9:16 tagliata dalla stessa foto con `scripts/media/hero-piscina.mjs` (Raffaela intera in ogni caso), e dichiaralo.

Test che il blocco tocca (rileggili prima): `intro-clocks.test.ts`, `corridor-contract.test.ts` (il corridoio `hero` nel CSS e nel hook), `chapters.test.ts` + `app/lib/motion/chapters.ts` (firma `hero`: «tuffo» → il gesto nuovo; `CorridorId`), `data-bg.test.ts` (il marcatore da 1 px dell'hero → le bande di hero.json come `.dt-od_soggetto`), `split-title-migrazione.test.ts`, `media-file.test.ts`, `ambient-video.test.ts` se serve; e2e `hero-dive.spec.ts`, `preloader-corta.spec.ts`, `home.spec.ts` (capitolo 1), `corridors.spec.ts` (`EXPECTED_HOME` senza `hero`, `SCREEN_ROUTES`), `segno.spec.ts` (i marcatori dell'hero), `motion.spec.ts`, `mobile-motion.spec.ts` (atti III e IV: porta e tuffo), `a28.spec.ts`. Aggiorna anche `DESIGN.md` («Primo schermo e testata»), `PRODUCT.md`, il registro §11 (A49 → applicata con l'hash) e il handoff.

## Blocco 2 — A72: «Costi chiari» diventa un nastro (le mie parole: «togliamo il video della piscina, e mettiamo un'altra immagine no-bg alta: villa-facciata-piscina-alta-cielo.webp, stesso stile e animazione dello sticky scroll che poi diventa scroll orizzontale, ed entra la sezione di Carmine e Seguici»)

Oggi in `app/page.tsx`: `<CostiChiari acqua />` (occhiello «Quanto costa», h2 «Nessun costo anticipato.», lead, CTA, poi la banda video `acqua-1080.*` con `useAmbientVideo`), seguita da `<FeaturedTestimonial gesture />` (Carmine, la foto che affonda, firma `dtAffonda`) e `<Social />` («Seguici»).

Da costruire: la sezione dei costi monta `HorizonScroller` come la finestra:
- pannello 1: `public/images/reali/villa-facciata-piscina-alta-cielo.webp` (2560×3816, la stessa della testa di /vendi, cima 0,219 in `tinte.json`; misure con `node scripts/media/finestra.mjs villa-facciata-piscina-alta app/lib/motion/costi.json`) a tutta larghezza, intera, col titolo «Nessun costo anticipato» sul cielo-carta come «Open Domus» e la SALITA dietro il titolo (`lead`); occhiello, lead e CTA dei costi: decidi tu se restano sopra il nastro sulla carta o posano sulla foto in bianco (A70), e dichiaralo;
- pannello 2: Carmine (il blocco di `FeaturedTestimonial`, col suo video) con il sipario del nastro;
- pannello 3: «Seguici» (`Social`) con le icone e le anteprime; se vuoi una coda che scende 1:1 come la piscina di Open Domus, usa `tail` e la chiusura in cartolina (A68), altrimenti il nastro finisce lì;
- il video dell'acqua (`acqua-1080.mp4/webm`, `acqua-poster.jpg`) esce dal codice ma resta nel repo (i file non montati restano: mia risposta del 22 set.);
- registro dei capitoli: `chapters.ts` ha 17 voci della home con firme distinte (D18: ease e scrub diversi per capitolo, `chapters.test.ts`); `costi`, `testimonianza` e `social` vanno riletti: il nastro prende la firma di `costi` (o una nuova, con la sua CustomEase registrata in `gsap.ts`), Carmine e Seguici diventano pannelli; decidi tu e fai tornare i test (17 capitoli, inneschi diversi, «il registro combacia col codice dei nastri»);
- test che tocca: `chapters.test.ts`, `data-bg.test.ts` (la banda dell'acqua), `ambient-video.test.ts`, `media-file.test.ts`, `doc-services.test.ts` se legge `costi`; e2e `home.spec.ts` (capitoli 12-14), `segno.spec.ts`, `motion.spec.ts`, `corridors.spec.ts` (`EXPECTED_HOME`: il nastro nuovo ha `data-corridor`), `mobile-motion.spec.ts`, `a11y.spec.ts`.

## Come lavorare e verificare

- Il gate ECC nega SEMPRE il primo Edit/Write di ogni file in ogni turno: scrivi i fatti in una riga e riprova; le patch a script vanno solo per blocchi lunghi (`scripts/.probe/`, non in repo).
- Giudico sul dev server Turbopack (porta 3000, spesso già acceso da un'altra chat): agganciati con `preview_start { url: "http://localhost:3000" }`; la CSS di Turbopack può essere stantia, e le quote misurate nel pane cambiano se il pane si ridimensiona. Le prove vere sono `npm test` (unit), `npx tsc --noEmit`, `npx eslint <file>` e gli e2e sul build: `npx playwright test --config=playwright.site.config.ts e2e/home.spec.ts -g "…" --workers=3 --reporter=line` (fa `next build` da sé sulla 3177, ~3 min; una corsa alla volta). `wheelTo` degli e2e può sforare di ~80 px; la sezione di Voci si accorcia di ~80 px dopo il primo passaggio e ScrollTrigger non rifà il refresh: nei test rileggi le quote dal DOM prima di ogni checkpoint.
- Commit a blocchi (messaggio in italiano, cosa e perché, `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`), poi un commit `docs(registro): hash …` che mette l'hash nel §11 e nel handoff. Non spingere senza che io lo dica.
- Higgsfield: restano 5 crediti; non spenderli senza dirmelo.
- Restano aperti, da non toccare senza il mio sì: le otto segnalazioni dell'hook di design su globals.css (letterali preesistenti) e `impeccable document`; il push su PR #79; la causa degli 80 px di Voci; le scritte bianche sulle teste (A70) le testo io.

Parti dal blocco 1. Alla fine di ogni blocco dimmi cosa hai costruito, cosa hai deciso tu e perché, cosa hai verificato e con quali numeri, e cosa manca.
